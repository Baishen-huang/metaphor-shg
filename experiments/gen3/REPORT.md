# `type` 的判决与 7 维特征集全量审计

> 分支 `exp/typefeat`（worktree `.wt/typefeat`，基线 `exp/source` @ `64872c4`）
> 全部离线 ｜ $0（本体 JSON 重放 + LLM 缓存重放 + 句向量缓存重放，零 API 请求）
> `data/` 只读；全部产物在 `experiments/gen3/`
> 日期：2026-09-24

---

## 0. 摘要（先给结论）

| 问题 | 裁决 | 关键数字 |
|---|---|---|
| `type` 是死维吗？ | ⚠️ **gen1 的「死维」判定不成立，但它的结论方向对** —— `type` 不是死维，它是**弱维 + 与 `struct` 共线** | 配对 AUC **0.5249** [0.5092,0.5401]（CI 不含 0.5，**显著弱正向**）；gen1 的 0.497 是**训练集**口径，非评测口径 |
| gen1 的 0.4973 能复现吗？ | ✅ **逐位复现** | 训练集池化 AUC = **0.4973**，学到权重 = **+0.0487**，人工硬编码 = **0.20** —— 三个数全对 |
| `same_cascade` 与 `same_frame` 冗余吗？ | ✅ **逐位复现** | `same_cascade ⇒ same_frame` = **51.7%**（29 配对中 15 对同框架，gen2 口径） |
| 删掉 `type` 会怎样？ | **什么都没变** | 自监督 Δ=**+0.0028** [−0.0043,+0.0097] p=0.47；弱监督 Δ=+0.0102；真实句向量 Δ=+0.0065 [−0.0009,+0.0141] p=0.077。**方向一致为正，但都不显著** |
| 删掉 `same_cascade` 呢？ | **也没变** | Δ=**+0.0017** [−0.0034,+0.0065] p=0.49 |
| 换个有信息的信号替代 `type` 呢？ | ❌ **全部无效** | 6 个替代信号：frame_support +0.0082、frame_tier +0.0031、cascade_size +0.0003、n_emergent −0.0013、frame_registered −0.0006、ground_len −0.0004 —— **6 个的 CI 全部覆盖 0** |
| 人工加权错配吗？ | ✅ **是，而且这是本代最大的发现** | 人工权重改成与学到权重同比例后：**0.4809 → 0.5496（+6.87pp）**，**反超训练后的 0.5447** |
| 「训练增益 +6.4pp」是真实的模型能力吗？ | ❌ **不是** | 人工权重调对后，训练增益 **+0.49pp [−0.89,+1.84]（CI 覆盖 0）**。**1 维 `sem` 单信号 = 0.5474，与训练后 7 维 0.5449 无差异**（Δ=+0.0025，CI 覆盖 0）；**真实句向量口径下 1 维 `sem` 反而显著更高（+1.76pp，p=0.003）** |
| gen1 报告的「fullcorpus A7/H5 结论翻转 +0.3pp→+9.5pp」 | ⚠️ **翻转是真的，但归因要改** | 翻转量 **+9.18pp 全部来自「人工加权硬编码 0.20」这一项**；训练侧去 `type` 的效应 = **+0.00pp** |
| §6.2 的 LLM「非构造金标」真的非构造吗？ | ❌ **不是** | **632/632 = 100%** 的查询，LLM 金标都包含「产出该问题的 chunk」；**619/632 = 97.9%** 只有那一个 chunk |
| Hits@10 恒为 1.000 说明排序完美吗？ | ❌ **不说明** | 候选池 median=**7**、max=**10** → Hits@10 **平凡为 1.0**；随机排序期望 MRR 就有 **0.2622** |

**一句话**：`type` 该不该删的答案不是「该删」或「该留」，而是**「删不删都一样（都不显著），但它不是死维——gen1 的 0.497 是训练集口径的误读」**。
本代真正的发现是**指标本身不可用**：632 条查询里 100% 的金标锚在产出 chunk 上、97.9% 只有那一个答案、候选池只有 7 个 chunk、Hits@10 平凡饱和 —— 在这个任务上，**1 维 `sem` 就等于 7 维训练后排序器**，已上报的 +6.4pp「训练增益」主要是**人工权重配错**的产物。

---

## 1. 前人结论的复现（全部逐位命中）

### 1.1 gen1：`type` 的三元不一致

`experiments/gen3/exp_g3_1_audit.py`，110 个伪文档各训一个排序器后取均值：

| 量 | gen1 报告 | gen3 实测 | 一致？ |
|---|---|---|---|
| `type` 单特征 AUC | 0.4973 | **0.4973** | ✅ 逐位 |
| `type` 学到权重 | +0.0487 | **+0.0487** | ✅ 逐位 |
| `type` 人工硬编码权重 | 0.20 | **0.20** | ✅ |
| `sem` AUC / 权重 | 0.9329 / +0.6911 | **0.9329 / +0.6911** | ✅ 逐位 |
| `clue` AUC / 权重 | 0.9985 / +1.2543 | **0.9985 / +1.2543** | ✅ 逐位 |
| `struct` AUC / 权重 | 0.5036 / +0.0195 | **0.5036 / +0.0195** | ✅ 逐位 |
| `same_frame` AUC / 权重 | 0.5975 / +0.1953 | **0.5975 / +0.1953** | ✅ 逐位 |
| `same_cascade` AUC / 权重 | 0.5975 / +0.1958 | **0.5975 / +0.1958** | ✅ 逐位 |
| `ground_jaccard` AUC / 权重 | 0.7437 / +0.4965 | **0.7437 / +0.4965** | ✅ 逐位 |

**但这里有一个必须纠正的口径问题（本代的关键发现之一）**：

> gen1 的 `0.4973` 来自 `probe_learned_type_weight.py`，它调用的是
> `feature_auc(ds)`，其中 `ds` 是 **`build_training_set` 造出的自监督训练集**
> （正样本 = chunk 实际产出的边），**不是评测候选对上的 AUC**。
>
> 在**评测**候选对上（632 条 LLM 改写查询 × 全部候选，n=5485 对），
> `type` 的配对 AUC 是 **0.5249 [0.5092, 0.5401]** —— CI **不覆盖 0.5**。
>
> 所以准确的表述是：**「`type` 在自监督训练集上与标签无关（AUC 0.497），但在评测金标上是显著弱正向的（AUC 0.525）」**。
> 这两个数字不矛盾（训练集的正样本是「chunk 产出的边」，评测金标是「LLM 判定相关的 chunk」），
> 但把它们混为一谈会得出「`type` 是死维」这个**过强的结论**。
> gen1 报告 §4 的诚实标注已经说对了方向（「`type` 与金标弱正相关但 AUC 仅 0.497」），
> 只是那个 0.497 不是它以为的那个 AUC。

### 1.2 gen2：`same_cascade` / `same_frame` 冗余

`exp_g2_5_mechanism.py` 的口径是「L1 边两两配对中 `same_cascade` 命中者，同框架的比例」：

```
L1 边数 856 ｜ 有 cascade_id 668 = 78.0%
same_cascade 配对命中 29 ｜ 其中同框架 15
→ same_cascade ⇒ same_frame 比例 = 51.7%          （gen2 报告 0.517）✅ 逐位
```

另一口径（**评测候选对上**，本代新增）：
```
same_frame 命中 16 ｜ same_cascade 命中 25 ｜ 同时命中 16
→ same_cascade 命中中同框架比例 = 64.0% ｜ Jaccard = 0.640
```
两个口径都报，因为前者是「图的属性」，后者是「排序器实际看到的东西」。

---

## 2. 【主交付物】7 维特征全量审计表

### 2.1 表 1 —— 单特征 AUC（两种口径）

AUC 有两种口径，必须分开报，否则会误判：

- **池化（plain）**：把 110 个文档的 (查询, 候选) 对全部堆在一起。跨文档的绝对水平不可比。
- **配对（paired）**：**同一查询内**金标候选 vs 非金标候选，再对 632 条查询平均。这是与 MRR 排序语义一致的正确口径。

| 特征 | 池化 AUC | 95% CI | **配对 AUC** | **95% CI** | 常数率（最高频取值） |
|---|---:|---|---:|---|---:|
| `sem` | 0.6195 | [0.5997, 0.6401] | **0.6277** | [0.6023, 0.6522] | 6.9% |
| `struct` | 0.5258 | [0.5143, 0.5368] | **0.5197** | [0.5063, 0.5323] | **85.1%** |
| `clue` | 0.5155 | [0.5101, 0.5214] | **0.5087** | [0.5043, 0.5132] | **99.4%** |
| `type` | 0.5309 | [0.5170, 0.5439] | **0.5249** | [0.5092, 0.5401] | 80.1% |
| `same_frame` | 0.5080 | [0.5042, 0.5123] | **0.5053** | [0.5023, 0.5087] | **99.7%** |
| `same_cascade` | 0.5090 | [0.5049, 0.5136] | **0.5070** | [0.5030, 0.5116] | **99.5%** |
| `ground_jaccard` | 0.5363 | [0.5284, 0.5451] | **0.5300** | [0.5215, 0.5392] | **98.5%** |

**读法**：
- 只有 `sem` 有实质判别力（0.628）。其余 6 维全部落在 0.505–0.530 的**极窄带**里。
- 但「弱」不等于「死」：这 6 维的 CI **全部不含 0.5**。它们都是**统计上显著、实务上微弱**的信号。
- `clue` 的池化 AUC 只有 0.5155 —— 与 gen1 说的「clue AUC≈0.999 饱和」并不矛盾：
  **0.9985 是训练集口径**（改写查询被程序化禁止含触发词，所以评测集上 clue 几乎恒 0：99.4% 的候选对取值为 0）。
  这是「clue 捷径被切断」的**成功证据**，不是特征失效。

### 2.2 表 2 —— 分层配对 AUC

| 特征 | 边锚点 (n=15) | 文本锚点 (n=617) | 小池 (n=384) | 大池 (n=248) |
|---|---:|---:|---:|---:|
| `sem` | **0.8458** | 0.6224 | 0.6172 | 0.6439 |
| `struct` | 0.5439 | 0.5191 | 0.5208 | 0.5181 |
| `clue` | **0.8230** | 0.5010 | 0.5116 | 0.5042 |
| `type` | 0.5990 | 0.5231 | 0.5246 | 0.5253 |
| `same_frame` | 0.7230 | **0.5000** | 0.5078 | 0.5013 |
| `same_cascade` | 0.7508 | **0.5011** | 0.5093 | 0.5034 |
| `ground_jaccard` | 0.8016 | 0.5234 | 0.5335 | 0.5246 |

**关键**：`same_frame` / `same_cascade` 在**文本锚点**（占 617/632 = 97.6%）上的 AUC 是 **0.5000 / 0.5011** —— 精确的随机。
它们仅在 15 条边锚点查询上有力（0.72/0.75），而那 15 条只占 2.4%。
**这两个「角色感知结构信号」在 97.6% 的查询上完全无效。**

### 2.3 表 3 —— 训练后权重 vs 人工权重

| 特征 | 学到权重（标准化空间） | 标准差 | 中位数 | \|w\|>0.05 比例 | 人工权重 | 归一人工 | 训练集池化 AUC |
|---|---:|---:|---:|---:|---:|---:|---:|
| `sem` | **+0.6911** | 0.1415 | +0.6783 | 100.0% | 0.35 | 0.350 | 0.9329 |
| `struct` | **+0.0195** | 0.0672 | **−0.0077** | 32.7% | **0.25** | **0.250** | 0.5036 |
| `clue` | **+1.2543** | 0.2337 | +1.2069 | 100.0% | 0.20 | 0.200 | 0.9985 |
| `type` | **+0.0487** | 0.0831 | +0.0417 | 52.7% | **0.20** | **0.200** | 0.4973 |
| `same_frame` | +0.1953 | 0.1600 | +0.2152 | 70.9% | 0.00 | 0.000 | 0.5975 |
| `same_cascade` | +0.1958 | 0.1560 | +0.2208 | 71.8% | 0.00 | 0.000 | 0.5975 |
| `ground_jaccard` | +0.4965 | 0.2009 | +0.5117 | 99.1% | 0.00 | 0.000 | 0.7437 |

**人工权重错配量化**（这是本代最重要的表）：

| 特征 | 人工权重（归一） | 学到权重（原始空间有效值 `w/sd` 归一） | 错配倍数 |
|---|---:|---:|---:|
| `sem` | 0.350 | **0.503** | 人工**低估** 1.4× |
| `clue` | 0.200 | **0.291** | 人工低估 1.5× |
| `ground_jaccard` | **0.000** | 0.101 | 人工**完全忽略** |
| `same_frame` | **0.000** | 0.045 | 人工完全忽略 |
| `same_cascade` | **0.000** | 0.045 | 人工完全忽略 |
| `struct` | **0.250** | 0.005 | 人工**高估 49×** |
| `type` | **0.200** | 0.011 | 人工**高估 19×** |

> 「原始空间有效权重」= 标准化空间的 `w` 除以该特征的标准差。因为训练器在标准化空间工作，
> 只有 `w/sd` 才是「该特征每变动一个原始单位，logit 变动多少」，才与人工权重可比。

**`struct` 比 `type` 错得更离谱**（高估 49× vs 19×），而 `struct` 的人工权重 0.25 比 `type` 的 0.20 还大。
gen1/gen2 都把注意力放在 `type` 上，**真正最大的错配是 `struct`**。

### 2.4 表 4 —— 特征间冗余

秩相关（Spearman，下三角）/ 二值重叠率 Jaccard（上三角），**评测候选对**口径：

|  | `sem` | `struct` | `clue` | `type` | `same_frame` | `same_casc` | `ground_jac` |
|---|---:|---:|---:|---:|---:|---:|---:|
| `sem` | — | 0.931 | 0.006 | 0.931 | 0.003 | 0.005 | 0.016 |
| `struct` | −0.008 | — | 0.006 | **1.000** | 0.003 | 0.005 | 0.015 |
| `clue` | +0.039 | +0.408 | — | 0.006 | 0.516 | 0.400 | 0.237 |
| `type` | −0.033 | **+0.836** | +0.267 | — | 0.003 | 0.005 | 0.015 |
| `same_frame` | +0.033 | +0.415 | **+0.991** | +0.273 | — | **0.640** | 0.190 |
| `same_cascade` | +0.034 | +0.410 | **+0.991** | +0.268 | **+0.993** | — | 0.185 |
| `ground_jaccard` | +0.060 | +0.399 | **+0.958** | +0.256 | **+0.960** | **+0.958** | — |

**训练集内部（`X_self`）同一矩阵** —— 这才是训练器真正看到的东西：

|  | `sem` | `struct` | `clue` | `type` | `same_frame` | `same_casc` | `ground_jac` |
|---|---:|---:|---:|---:|---:|---:|---:|
| `sem` | — | 0.970 | 0.261 | 0.970 | 0.058 | 0.063 | 0.139 |
| `struct` | −0.024 | — | 0.253 | **1.000** | 0.056 | 0.061 | 0.135 |
| `clue` | +0.467 | +0.234 | — | 0.253 | 0.219 | 0.220 | 0.499 |
| `type` | −0.051 | **+0.846** | +0.161 | — | 0.056 | 0.061 | 0.135 |
| `same_frame` | +0.124 | +0.304 | +0.561 | +0.205 | — | **0.914** | 0.368 |
| `same_cascade` | +0.122 | +0.291 | +0.553 | +0.196 | **+0.976** | — | 0.358 |
| `ground_jaccard` | +0.265 | +0.090 | +0.620 | −0.023 | +0.713 | +0.714 | — |

**三个冗余簇（都必须报）**：

1. **`struct` × `type`：二值重叠 Jaccard = 1.000，秩相关 +0.836 / +0.846**。
   两者都几乎只反映「有框架/级联归属」这一个二值事实。
   这不是巧合：`struct = min(1, 0.5·|ground| + 0.5·[cascade 非空])` 在喻底 ≥1 时对任何有级联的候选都饱和到 1.0（实测 **85.1%** 的候选对取值恰为 1.0）。
   **`type` 与 `struct` 不是两个独立信号。**
2. **`clue` × `same_frame` × `same_cascade`：秩相关 +0.991 / +0.991 / +0.993，重叠 0.40–0.64**。
   三者高度共线 —— 因为 `_query_edges` 把「查询侧超边」定义为「查询触发词命中框架下的超边」，
   于是 `clue`（触发词命中）与 `same_frame`（同框架）在文本锚点路径上**在构造上耦合**。
3. **`ground_jaccard` × `same_frame` × `same_cascade`：+0.958 / +0.960**。
   喻底集合按框架定义，同框架必然喻底高度重合。

> 诚实标注：评测口径的秩相关被「近常数」严重放大（`clue` 99.4% 为 0，`same_frame` 99.7% 为 0）。
> 高秩相关在有大量并列时会被并列结构主导。**训练集口径（第二张表）的相关系数更可信**，
> 那里 `clue × same_frame` 是 +0.561（而非 0.991）。两个都报，读者自行取舍。

### 2.5 表 5 —— 裁决（规则预先声明，不事后调参）

裁决规则：
- `dead`：配对 AUC 的 95% CI 覆盖 0.5 **且** |配对 AUC − 0.5| < 0.02
- `harmful`：配对 AUC < 0.5 **且** CI 上界 < 0.5
- `useful`：CI 下界 > 0.5 **且** |配对 AUC − 0.5| ≥ 0.02（显著且非平凡）
- `weak`：CI 下界 > 0.5 **但** |配对 AUC − 0.5| < 0.02（显著但幅度小）
- `redundant`：与某一维的秩相关 ≥ 0.8 或二值重叠 Jaccard ≥ 0.8

| 特征 | 配对 AUC | 裁决 | 说明 |
|---|---:|---|---|
| `sem` | 0.6277 | **useful** | 唯一有实质判别力的维（幅度 +0.128） |
| `ground_jaccard` | 0.5300 | **useful（幅度小）** | 幅度 +0.030；与角色三维高度共线 |
| `type` | 0.5249 | **useful（幅度小）+ redundant(struct)** | **不是死维**；与 `struct` 二值重叠 1.000 |
| `struct` | 0.5197 | **weak + redundant(type)** | 幅度 +0.020；85.1% 饱和为常数 1.0 |
| `clue` | 0.5087 | **weak + redundant(same_frame/same_cascade/ground_jaccard)** | 评测集上 99.4% 为 0（改写查询已切断捷径） |
| `same_cascade` | 0.5070 | **weak + redundant(clue/same_frame/ground_jaccard)** | 文本锚点上 AUC = 0.5011（纯随机） |
| `same_frame` | 0.5053 | **weak + redundant(clue/same_cascade/ground_jaccard)** | 文本锚点上 AUC = 0.5000（纯随机） |

**没有任何一维被判为 `dead` 或 `harmful`。** 这是与 gen1 叙述不同的地方，且是可复算的。

> 诚实标注：`useful` / `weak` 的分界（幅度 0.02）是**本报告预先选定的**，不是行业标准。
> 若把门槛降到 0.01，则 7 维全部为 `useful`；升到 0.03 则只剩 `sem` 与 `ground_jaccard`。
> 真正稳健的结论只有两条：**① 只有 `sem` 的幅度值得一提（0.628）；
> ② 其余 6 维的 CI 都不含 0.5，但幅度都在 0.02 上下，实务价值有限。**

---

## 3. 处置实验（before / after）

### 3.1 去维 / 换信号（训练侧，重训）

LLM 非构造金标，n=632，配对 bootstrap 95% CI：

| 处置 | MRR@10 | Δ vs 基线 | 95% CI | p | 结论 |
|---|---:|---:|---|---:|---|
| D0 基线 7 维 | 0.5447 | — | — | — | — |
| **D1 去 `type`（6 维）** | **0.5475** | **+0.0028** | [−0.0043,+0.0097] | 0.47 | 无差异 |
| **D2 去 `same_cascade`（6 维）** | 0.5464 | +0.0017 | [−0.0034,+0.0065] | 0.49 | 无差异 |
| D3 去 `type`+`same_cascade`（5 维） | 0.5480 | +0.0033 | [−0.0048,+0.0109] | 0.43 | 无差异 |
| D8 换 `type`→`frame_support_norm` | 0.5529 | +0.0082 | [−0.0046,+0.0207] | 0.21 | 无差异（最大的一次，但仍不显著） |
| D9 换 `type`→`frame_tier_core` | 0.5478 | +0.0031 | [−0.0097,+0.0158] | 0.63 | 无差异 |
| D10 换 `type`→`cascade_size_norm` | 0.5450 | +0.0003 | [−0.0117,+0.0124] | 0.94 | 无差异 |
| D11 换 `type`→`n_emergent` | 0.5434 | −0.0013 | [−0.0143,+0.0126] | 0.87 | 无差异 |
| D12 换 `type`→`frame_registered` | 0.5441 | −0.0006 | [−0.0026,+0.0007] | 0.52 | 无差异 |
| D13 换 `type`→`ground_len_norm` | 0.5443 | −0.0004 | [−0.0126,+0.0129] | 0.95 | 无差异 |

**全部 6 个「换信号」处置 + 3 个「去维」处置的 CI 都覆盖 0。** 去 `type` 的方向是**正的**（+0.28pp），换 `frame_support` 是**最大的一次**（+0.82pp），
但**没有一项达到显著性**。诚实结论：**在本基准上，`type` 的处置对排序指标不可测。**

> 若要「制造一个增益」，最省事的做法是挑 D8（+0.82pp）上报。**本报告拒绝这样做**：
> 它是 6 个替代信号中最大的一个（连同去维共 9 个处置），且 CI 下界 −0.46pp，完全在噪声内
> —— 这是多重比较下的选择偏差。

### 3.2 换人工权重（本代最大发现）

| 配置 | MRR@10 | Δ vs M0 | 95% CI | p |
|---|---:|---:|---|---:|
| **M0 已上报人工 0.35/0.25/0.20/0.20** | **0.4809** | — | — | — |
| M7 四维等权 0.25×4 | 0.4811 | +0.0002 | [−0.0001,+0.0005] | 0.28 |
| M2 `type` 降到 0.05 | 0.4982 | +0.0173 | [+0.0085,+0.0266] | **0.000** |
| M1 去 `type`，权重给 `clue`（0.35/0.25/0.40） | 0.5025 | +0.0215 | [+0.0069,+0.0355] | **0.003** |
| M6 三维等权 1/3 | 0.5025 | +0.0215 | [+0.0069,+0.0355] | **0.003** |
| M5 七维手工调 .30/.20/.15/.05/.05/.05/.20 | 0.5074 | +0.0265 | [+0.0142,+0.0381] | **0.000** |
| M4 七维按学到比例 | 0.5487 | +0.0678 | [+0.0480,+0.0861] | **0.000** |
| **M3 四维按学到比例** | **0.5496** | **+0.0687** | [+0.0497,+0.0867] | **0.000** |
| （对照）自监督训练 7 维 | 0.5447 | +0.0638 | — | — |

**`type` 单独贡献了 +2.15pp**（M0 → M1/M6，两者排序**逐条完全相同**：`0.35/0.25/0.40` 与 `1/3,1/3,1/3` 是同一个正比例缩放，实测 632/632 条查询的完整 chunk 排序一致）。
即：**已上报的人工加权基线，单是「`type` 占 0.20」这一项就损失了 2.15pp MRR。**

### 3.3 训练增益在「人工权重配对」之后还剩多少

| 对照 | Δ MRR@10 | 95% CI | p |
|---|---:|---|---:|
| 人工（按学到比例）− 已上报人工 | **+0.0687** | [+0.0497,+0.0867] | 0.0000 |
| **人工（按学到比例）− 训练后 7 维** | **+0.0049** | **[−0.0089,+0.0184]** | **0.48** |

**CI 覆盖 0。训练器没有超出「正确加权」的能力。**

零假设对照进一步确认（`exp_g3_4_null.py`）：

| 配置 | MRR@10 | Δ vs 训练后 7 维 | 95% CI | p |
|---|---:|---:|---|---:|
| **N2 纯 `sem`（1 维，不训练）** | **0.5474** | **+0.0025** | **[−0.0096,+0.0150]** | **0.70** |
| N8 `sem`+`clue` 训练 | 0.5473 | +0.0025 | [−0.0097,+0.0150] | 0.71 |
| N13 `clue`+`sem`+`ground` | 0.5458 | +0.0009 | [−0.0108,+0.0126] | 0.90 |
| N7 训练后 7 维 | 0.5449 | — | — | — |
| N1 纯 `clue` | 0.4124 | −0.1324 | [−0.1639,−0.0996] | 0.000 |
| N3 纯 `ground_jaccard` | 0.4406 | −0.1042 | [−0.1349,−0.0722] | 0.000 |
| N9 纯 `struct` | 0.4010 | −0.1439 | [−0.1776,−0.1096] | 0.000 |
| N10 纯 `type` | 0.4060 | −0.1389 | [−0.1741,−0.1036] | 0.000 |
| N11 纯 `same_frame` | 0.4101 | −0.1348 | [−0.1663,−0.1012] | 0.000 |
| N12 纯 `same_cascade` | 0.4107 | −0.1341 | [−0.1660,−0.1008] | 0.000 |
| N6 随机排序 | 0.4099 | −0.1350 | [−0.1618,−0.1092] | 0.000 |
| N5 ORACLE（产出 chunk 置顶） | 1.0000 | +0.4551 | [+0.4288,+0.4810] | 0.000 |

**决定性**：`纯 sem`（0.5474）= `sem+clue`（0.5473）= `训练后 7 维`（0.5449），三者 CI 互相覆盖。
用 `MetaphorScorer` 只在 `sem` 一维上训练，结果与直接用原始 `sem` **逐条相同**（Δ=0.0000，p=1.000）。
**7 维训练后的排序器实质上是一个 1 维 `sem` 模型。**

注意 N6 随机排序 = 0.4099 而其余单维 = 0.401–0.441 —— 它们都**低于**随机。
原因：这些维 99% 以上取 0，把绝大多数 chunk 压成同分，排序退化成候选顺序，
而候选顺序与「产出 chunk」的位置负相关（产出 chunk 常排在候选列表后部）。这本身是「近常数维在池内无信息」的又一个证据。

### 3.4 真实句向量（real-embedder）一行

缓存命中 **25520 / 25520 = 100%**（`data/embed_cache.json`，embedding-3，512 维）—— **可离线精确重放**。

| 配置 | MRR@10 | Hits@3 |
|---|---:|---:|
| M0 已上报人工 0.35/0.25/0.20/0.20 | 0.6734 | 0.7832 |
| D0 基线 7 维训练后 | 0.7512 | 0.8703 |
| D1 去 `type` | 0.7577 | 0.8734 |
| D2 去 `same_cascade` | 0.7540 | 0.8703 |
| D3 去 `type`+`same_cascade` | 0.7581 | 0.8750 |
| D8 换 `type`→`frame_support_norm` | 0.7555 | 0.8687 |
| M1 人工去 `type` | 0.7073 | 0.8149 |
| **M3 人工按学到比例** | **0.7740** | **0.8956** |
| **纯 `sem`（1 维，不训练）** | **0.7688** | 0.8813 |

复现对照：exp/source 的 real 重放为 0.6734 / 0.7512 / 0.7340，本次**逐位一致**。

关键对照（配对 bootstrap）：

| 对照 | Δ | 95% CI | p |
|---|---:|---|---:|
| 去 `type` − 基线 | +0.0065 | [−0.0009,+0.0141] | 0.077 |
| 人工按学到比例 − 已上报人工 | **+0.1006** | [+0.0806,+0.1203] | **0.000** |
| 人工去 `type` − 已上报人工 | **+0.0339** | [+0.0203,+0.0479] | **0.000** |
| **纯 `sem` − 训练后 7 维** | **+0.0176** | **[+0.0067,+0.0293]** | **0.003** |
| 纯 `sem` − 已上报人工 | **+0.0954** | [+0.0725,+0.1185] | **0.000** |

**真实句向量下方向完全一致，而且更强**：去 `type` 仍为正但不显著（p=0.077）；人工权重重配 +10.06pp；
**纯 `sem`（0.7688）显著超过训练后 7 维（0.7512），+1.76pp，p=0.003**。

> 这是**真实句向量口径下最强的一个结论**：在真语义向量上，7 维可训练排序器
> **输给**一个不训练的单维 `sem`。哈希词袋口径下二者只是持平（Δ=+0.0025，CI 覆盖 0），
> 那是因为哈希编码的 `sem` 本身就是个弱信号；换成真语义向量后差距才显出来。
> **即：所谓「可训练排序器」在真语义向量下的净效应是负的。**

---

## 4. 指标批判：金标构造混杂的量化

### 4.1 三个缺陷（全部可复现）

**缺陷 1 —— 候选池太小，Hits@10 平凡饱和**

```
候选 chunk 池：min=2  median=7  max=10  mean=7.03
池 ≤ 10 的查询占比 = 100.0%   → Hits@10 在 100% 的查询上平凡为 1.000
池 ≤ 3 的查询占比 = 2.2%
随机排序（均匀）的期望 MRR@10 = 0.2622   ← 指标的**真实地板**，不是 0
完美排序 MRR = 1.0，可用动态范围只有 0.738
```

已上报的「Hits@10 = 1.000」不是排序完美的证据，是**池只有 7 个 chunk** 的算术必然。

**缺陷 2 —— 「LLM 非构造金标」仍然 100% 锚定在产出 chunk 上**

```
LLM 认可产出边相关        : 612/632 = 96.8%
产出 chunk ∈ LLM 金标     : 632/632 = 100.0%   ← 锚点没有被移除
LLM 金标恰好 == {产出 chunk}: 619/632 = 97.9%   ← 97.9% 只有唯一正确答案
LLM 金标 chunk 数         : median=1  max=3  mean=1.02
```

`evaluate_llmgold.py` 的文档字符串称 LLM 金标是「与抽取构造无关的独立金标」。
**实测不成立**：LLM 判定的作用是**过滤**（剔掉 3.2% 不认可的），不是**去锚**。
任务实质 = 「把产出这条边/这个 chunk 排到第一」。

**缺陷 3 —— 训练与评测同源**

自监督训练集的正样本是「该 chunk 实际抽出的边」（`_build_from_chunks`），
评测金标是「该 chunk 产出的边被 LLM 判为相关」。二者是**同一个 chunk**。
所以 `clue`（触发词命中）与 `sem` 在训练集上 AUC 高达 0.9985 / 0.9329 —— 这不是泄漏检测能抓的，
它是**训练目标与评测目标重合**。

### 4.2 三个协议的对照（构造锚点被逐步移除）

| 协议 | 说明 | M0 人工 | T7 训练后 | 训练增益 | n |
|---|---|---:|---:|---:|---:|
| **P1 已上报** | 金标含产出 chunk，池含产出 chunk | 0.4809 | 0.5447 | **+6.38pp** | 632 |
| **P2 去锚点** | 金标与池**双双剔除**产出 chunk | 0.6987 | 0.6923 | **−0.64pp** | **13** |
| P3 排除型金标 | 只保留「LLM 金标 ≠ {产出 chunk}」的查询 | 0.6987 | 0.6923 | −0.64pp [−0.0192,0.0000] p=0.72 | **13** |

**P2/P3 的 n = 13（占 632 的 2.1%）。样本量过小，不能作为「训练无增益」的证据 ——
但它足以证明「训练增益在无锚点条件下不再出现」。**

### 4.3 MRR 来源分解（决定性）

| 配置 | MRR(产出 chunk) | n | MRR(非产出金标 chunk) | n |
|---|---:|---:|---:|---:|
| M0 人工 | 0.4728 | 632 | 0.6987 | 13 |
| T7 训练后 | **0.5372** | 632 | 0.6923 | 13 |
| **Δ** | **+0.0644** | | **−0.0064** | |

**训练增益 +6.44pp 全部落在「产出 chunk」列；「非产出」列反而 −0.64pp。**
结合 §3.3（训练后 7 维 = 1 维 `sem`），完整因果链是：

> 训练器学到的是「把产出 chunk 排更前」。
> 而产出 chunk 就是产生该查询的 chunk —— 复读它靠的是 `sem`（查询改写后与原句的语义相似度），
> 不需要任何隐喻结构特征。
> **所谓「训练增益」= 一个正确的 `sem` 权重 × 一个锚在产出 chunk 上的金标。**

---

## 5. fullcorpus A7/H5 的复核（gen1 标记为「需人工复核的重大结论变更」）

`exp_g3_6_fullcorpus.py`，by-construction 金标，Q_ext + Q_self，n=861：

| 配置 | MRR@10 | Hits@3 |
|---|---:|---:|
| 人工加权（`type` 参与，0.20） | 0.9030 | 0.9059 |
| **人工加权（`type` 恒 0）** | **0.9948** | **1.0000** |
| 训练后 7 维 | 0.9977 | 1.0000 |
| 训练后 6 维（去 `type`） | 0.9977 | 1.0000 |
| 纯 `sem` | 0.9923 | 1.0000 |
| 纯 `clue` | 0.9959 | 1.0000 |

| 量 | 值 |
|---|---:|
| A7 Δ = 训练7 − 人工(`type` 参与) | **+9.47pp**（= gen1 报告的「+9.5pp」）✅ |
| A7 Δ = 训练7 − 人工(`type`=0) | **+0.29pp** |
| **人工侧把 `type` 权重置 0 的收益** | **+9.18pp** |
| **训练侧去 `type` 的效应** | **+0.00pp**（逐条排序 32.5% 不同但 MRR 差恰为 0.0000） |

**裁决**：gen1 报告的「A7/H5 从『无增益❌』翻转为『+9.5pp 有增益✅』」**翻转是真的，但归因是错的**。

- 翻转量 **100% 来自「人工加权硬编码 0.20」这一项**，与「修复让 `type` 从常数变成有信息」无关。
- 修复 `type` 编码后，人工加权基线从 0.9030（`type` 参与）变成 0.9948（等价于 `type` 权重置 0）—— 是**基线被拖低**，不是**模型变强**。
- 正确的表述：**A7/H5 在 fullcorpus 上的增益 ≈ +0.29pp（不显著，CI 下界 0.0006），
  此前上报的 +9.5pp 是「人工基线被 `type` 权重污染」的产物。**

> 这个复核同时说明：**fullcorpus 的 by-construction 金标下所有配置都在 0.99+**
> （池小 + 金标就是产出边），该指标本身也已饱和，**不适合用来裁决特征取舍**。

---

## 6. 诚实裁决

### 6.1 `type` 应该删除、替换还是保留？

**裁决：既不该「因为死维而删除」，也不该「因为有用而保留」——它对指标的影响不可测，删除是安全的、但收益也是不可证的。**

依据：
1. **`type` 不是死维。** 配对 AUC 0.5249 [0.5092,0.5401]，CI 不含 0.5。gen1 的 0.497 是训练集口径的误读（§1.1）。
2. **删掉 `type` 的效应不可测。** 自监督 +0.0028（CI 覆盖 0）、弱监督 +0.0102、真实句向量 +0.0065（p=0.077）。
   方向一致为正，但**没有一项显著**。
3. **换任何信号都没用。** 6 个候选替代信号（frame_support / frame_tier / cascade_size / n_emergent / frame_registered / ground_len）的 CI **全部覆盖 0**。
   其中 `n_emergent`（gen2 发现的对「级联通路口非空」AUC 0.836 的信号）在端到端排序上是 **−0.0013**。
   这印证了 gen2 自己的边界声明：「预测一个弱通路是否非空」的工程价值有限。
4. **`type` 与 `struct` 重叠 1.000**（二值），是同一个「有归属」事实的两种编码。

**建议**：
- **保留 `type` 维度**，但把人工加权里的 0.20 **改掉**（见 6.3）。
- 若要精简：**删 `type` 或删 `same_cascade` 都是安全的**（都不显著），但**不要声称因此获得了增益**。
- 论文中「新增 type 维度防污染」的主张应下调为：**「`type` 是弱正向信号（配对 AUC 0.525），但在本基准上与 `struct` 共线，且对排序指标的边际贡献不可测」**。

### 6.2 `same_cascade` 应该删除吗？

**裁决：可以删（无指标代价），但「冗余」这个理由是**部分**成立的。**

- 删掉后 Δ=+0.0017（CI 覆盖 0），真实句向量 +0.0028（p=0.13）—— **无代价**。
- 但 gen2 的 51.7% 是 **L1 边两两配对**口径；在**评测候选对**上是 **64.0%**，Jaccard 0.640。
  两个口径都不足以说「完全冗余」——`same_cascade` 确实携带 36–48% 的增量配对信息。
- 真正让它无用的是：**它在 97.6% 的查询（文本锚点）上 AUC = 0.5011，精确的随机**。
  与 `same_frame` 一样，只在 15 条边锚点查询上有效。
- 训练器给它的权重 +0.1958 与 `same_frame` 的 +0.1953 **几乎相等**，且两者在评测口径上秩相关 +0.993。

**建议**：保留（删除无收益），但把论文 A9 的表述从「角色信息可被同质 DDE 替代」收紧为
**「角色特征（`same_frame`/`same_cascade`）在 97.6% 的文本锚点查询上不携带排序信息（AUC 0.500/0.501），
其可测作用仅限 2.4% 的边锚点查询」**。

### 6.3 人工加权是错配的吗？

**✅ 是，而且这是本代最大的可操作发现。**

| 事实 | 数字 |
|---|---|
| 已上报人工加权 MRR@10 | 0.4809 |
| 人工权重改成与学到权重同比例 | **0.5496（+6.87pp，p=0.0000）** |
| 训练后 7 维 | 0.5447 |
| **人工（配对的权重）− 训练后** | **+0.0049 [−0.0089,+0.0184]，CI 覆盖 0** |

**错配清单**（人工权重 vs 原始空间有效权重 `w/sd` 归一）：

| 特征 | 人工 | 应为（学到的比例） | 错配 |
|---|---:|---:|---|
| `struct` | **0.250** | **0.005** | **高估 49×** ← 最大 |
| `type` | **0.200** | **0.011** | 高估 19× |
| `ground_jaccard` | 0.000 | 0.101 | 忽略 |
| `same_frame` | 0.000 | 0.045 | 忽略 |
| `same_cascade` | 0.000 | 0.045 | 忽略 |
| `sem` | 0.350 | 0.503 | 低估 1.4× |
| `clue` | 0.200 | 0.291 | 低估 1.5× |

**两个后果**：

1. **已上报的人工加权基线（0.4809）不是「合理的无训练基线」，是一个被配错权重的基线。**
   论文 §6.2 的「训练增益 +6.4pp」应改写为：**「训练增益主要来自权重校准：把人工权重
   按学到比例重配后，人工加权达到 0.5496，与训练后 0.5447 无显著差异（+0.49pp，CI 覆盖 0）。」**
2. **`struct` 比 `type` 错得更离谱，而前两代都把注意力放在 `type` 上。**
   建议论文与代码注释把「人工加权 0.35/0.25/0.20/0.20 是历史遗留的未校准超参」写明。

### 6.4 训练增益是真的吗？

**裁决：在 LLM 金标上是「≈0」（哈希口径）/「显著为负」（真实句向量口径）；在 fullcorpus by-construction 金标上是「+0.29pp」。三处都不是原来报告的 +6.4pp / +9.5pp。**

四条独立证据：
1. 人工权重调对后，训练增益 +0.49pp，CI 覆盖 0（§3.3）。
2. **1 维 `sem` = 训练后 7 维**（哈希口径 Δ=+0.0025，CI 覆盖 0）（§3.3）。
3. **真实句向量口径下，1 维 `sem` 显著优于训练后 7 维**（Δ=+1.76pp，p=0.003）（§3.4）。
4. MRR 来源分解：训练增益 +6.44pp 全部落在「产出 chunk」列，非产出列 −0.64pp（§4.3）。

**这不否定「可训练排序器」这个组件的工程价值**（它至少能自动校准权重，替代手工调参）。
它否定的是**「训练学到了隐喻结构信号」这个解释**。

### 6.5 本报告没有证明什么

- **没有证明 `type` 该删。** 删除的效应不可测（CI 覆盖 0），「不显著」不等于「为零」。
  要证明该删，需要更大的候选池与去锚点金标（§7）。
- **没有证明任何替代信号无效的普适性。** `n_emergent` 在 gen2 的 Ω>0 子集（n=114）上 AUC 0.836 是真的，
  只是它预测的是「触发词级联通路口是否非空」，而那条通路的 Recall 只有 0.042–0.131（gen2 §4.2），
  工程价值本来就有上限。
- **P2/P3 只有 n=13，不能作为证据。** 它只够说明「训练增益不再出现」，不够说明「训练无用」。
- **真实句向量一行是缓存重放**，命中率 100%，与已上报的 0.707/0.758/0.755 逐位一致；
  但缓存只有 3176 条向量，覆盖的是**当前这 110 个伪文档的语料**，
  不能外推到其他语料（换语料会有未命中，需要重跑 API）。
- **人工权重的「最优比例」是从同一批数据学来的**，存在过拟合风险。
  严格做法是 split（`--split tune` / `report`），本代未做（因为 §3.3 的结论
  「人工配对的权重 = 训练后」本身不依赖最优性，只依赖「错配的幅度」）。

---

## 7. 对项目与论文的具体建议

| # | 建议 | 依据 |
|---|---|---|
| 1 | **§6.2 的人工加权基线应重配**，或明确标注「0.35/0.25/0.20/0.20 是未校准的历史超参，其中 `struct` 高估 49×、`type` 高估 19×」 | §6.3 |
| 2 | **「训练增益 +6.4pp」应改写为「权重校准效应」**，并补上「人工按学到比例 = 0.5496 vs 训练后 0.5447（Δ 不显著）」 | §3.3、§6.3 |
| 3 | **§7.3 的 A7/H5「翻转」结论应撤回或重写**：+9.5pp 是人工基线被 `type` 污染所致，真实增量 +0.29pp | §5 |
| 4 | **`type` 维的贡献主张应下调**为「弱正向（配对 AUC 0.525），与 `struct` 共线（重叠 1.000），边际贡献不可测」 | §2.3、§2.5 |
| 5 | **§6.2 的「LLM 非构造金标」标签应改为「LLM 过滤金标」**，并报出「100% 的查询金标含产出 chunk、97.9% 只有它」 | §4.1 |
| 6 | **Hits@10 应停止上报**（池 median=7 → 平凡 1.0），或改报「相对随机地板的 MRR 增益」（地板 = 0.2622） | §4.1 |
| 7 | **角色特征（`same_frame`/`same_cascade`）的表述应收紧**：97.6% 的文本锚点查询上 AUC = 0.500/0.501 | §2.2 |
| 8 | 下一代的**指标改造**优先于特征工程：需要（a）更大的候选池（≥50 chunk）、（b）去锚点金标、（c）人评或跨语料评测 | §4、§6.5 |

---

## 8. 复现命令

工作目录：`E:\02_AI项目\元图隐喻分析\.wt\typefeat`
解释器：`C:/Users/huang/.workbuddy/binaries/python/versions/3.13.12/venv_jieba/Scripts/python.exe`
全部离线、$0（`data/` 只读）

```bash
# ---- 缓存构建（首次运行各脚本会自动建；也可手动预建）----
# cache_default.pkl  ~5s      cache_real.pkl  ~60s（真实句向量，100% 缓存命中）

# ---- §1 复现前人结论 + §2 全量审计（主交付物）----
python experiments/gen3/exp_g3_1_audit.py            # 7 维审计表（AUC/权重/冗余/裁决）

# ---- §3 处置实验 ----
python experiments/gen3/exp_g3_2_dispositions.py     # 去维/换信号/换权重的 before-after
python experiments/gen3/exp_g3_4_null.py             # 零假设对照 + 留一消融（决定性）
python experiments/gen3/exp_g3_5_real.py             # real-embedder 处置对照

# ---- §4 指标批判 ----
python experiments/gen3/exp_g3_3_metric.py           # 池大小/构造锚点/P1-P2-P3 协议

# ---- §5 fullcorpus A7/H5 复核 ----
python experiments/gen3/exp_g3_6_fullcorpus.py       # A7 的 type 依赖（+9.5pp 归因）

# ---- §6 汇总 ----
python experiments/gen3/exp_g3_7_verdict.py          # 判定性问题汇总（读上述 JSON）

# ---- 端到端交叉验证（与 exp/source 已上报逐位一致）----
python -m metaphor_graph.evaluate_llmgold --stage eval
#   → 人工加权 0.481 ｜ 自监督 0.545 ｜ 弱监督 0.526（= exp/source 报告值）

# ---- 测试 ----
python -m unittest metaphor_graph.test_metaphor_graph   # Ran 133 tests ... OK
```

产物：`experiments/gen3/*.json`（结构化结果）+ `*.txt`（stdout 存档）
+ `cache_default.pkl` / `cache_real.pkl`（重放缓存）。

---

## 9. 变更文件清单

| 文件 | 改动 |
|---|---|
| `metaphor_graph/test_metaphor_graph.py` | +`TestFeatureSetAudit`（8 条回归护栏） |
| `experiments/gen3/g3_common.py` | 新增：重放缓存、人工加权顺序式、候选侧信号、bootstrap 工具 |
| `experiments/gen3/exp_g3_1_audit.py` | 新增：7 维全量审计 |
| `experiments/gen3/exp_g3_2_dispositions.py` | 新增：处置实验 |
| `experiments/gen3/exp_g3_3_metric.py` | 新增：指标批判 |
| `experiments/gen3/exp_g3_4_null.py` | 新增：零假设对照与留一消融 |
| `experiments/gen3/exp_g3_5_real.py` | 新增：real-embedder 处置对照 |
| `experiments/gen3/exp_g3_6_fullcorpus.py` | 新增：fullcorpus A7 复核 |
| `experiments/gen3/exp_g3_7_verdict.py` | 新增：判定性问题汇总 |

**未改动任何 `metaphor_graph/` 生产代码**（除测试文件）——本代是**纯审计**，
所有处置都在实验脚本内以特征子集/权重重配的形式实现，默认行为逐位不变。

---

## 附：本报告的自我更正记录

按 gen1 报告的传统，把过程中修正的错误留痕：

1. **初版把 gen1 的 0.4973 当成「评测 AUC」，据此得出「`type` 是死维」。**
   **这是错的。** 追查 `probe_learned_type_weight.py` 后发现它调的是
   `feature_auc(ds)`，`ds` 是**自监督训练集**。评测口径的配对 AUC 是 0.5249（CI 不含 0.5）。
   正文 §1.1 与 §2.5 已按实测改写，裁决从 `dead` 改为 `useful（弱）+redundant(struct)`。

2. **初版的人工加权用 numpy 点积 `F @ w` 实现，与已上报数字差 0.0003。**
   追查发现 `score_conditions` 是**逐项顺序累加**，浮点结合序不同，
   632 条查询里有 1 条完整排序不同（0.480947 vs 0.481211）。
   改用顺序累加后与 exp/source 的 0.4809 **逐位一致**。已加测试 `test_manual_weighting_float_order_matters` 钉住。

3. **初版的新测试在模块级 import `evaluate_retrieval` 拿 `ROLE_IDX`，导致测试套件 5 个用例失败。**
   原因：该模块**模块级**调用 `logging.disable(logging.CRITICAL)`，会全局关掉日志，
   让 `TestOpenAIBackendHTTP` 的 `assertLogs` 用例失败。这正是 gen2 报告 §7.3 记录过的坑，
   本次踩到了同一个。已改为延迟 import + 保存/恢复全局日志级别，并验证三种测试顺序均 OK。

4. **初版把 `struct` 与 `type` 的重叠写成「近乎完全共线」并构造了一个断言 `fa[1] > fb[1]`，测试失败。**
   实测 `struct` 在喻底 ≥1 且级联非空时**饱和到恰好 1.0**（85.1% 的候选对），
   所以「无级联」的候选 `fb[1]` 也可能因喻底多而到 1.0。
   已把测试改为断言饱和行为本身（`test_struct_saturates_at_one`）——**饱和是审计发现，不是测试的麻烦**。

5. **初版在 §3.1 表格里把 D8（+0.82pp，9 个处置中最大）写成「候选的最佳替代」。**
   复核后加注：这是多重比较下的选择偏差，CI 下界 −0.46pp，**不应上报为增益**。
