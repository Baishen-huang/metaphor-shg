# 第一代实验：跨方向结论汇总

> 本文件由人工维护（`collect.py --write` 只做机械抽取，不替代判读）。
> 每个方向返回后更新。数字均经独立复核。

第一代四个方向相互独立，检验四条已有线索。**其中两个方向证伪了提出它们的假设**
——包括我自己提出的两个。这是预期的结果形态：假设的价值在于被检验。

---

## 汇总表

| 方向 | 核心问题 | 裁决 | 是否改变已上报结论 |
|---|---|---|---|
| `exp/dynamics` | 无源项算子退化是否解释 GRU/HGNN 平局？ | ❌ **证伪**（平局非退化所致） | 否（结论稳健），但**新增 2 项负面结果** |
| `exp/omega` | 低 Ω 能否解释级联通路 0.042 失效？ | ❌ **否**（Ω 退化为 1-bit 指示器） | 否，但**定位了一个真实缺陷**（级联构造） |
| `exp/degrade` | 封顶可靠度能否让类型约束影响 P1？ | ❌ **否**（P1 是布尔存在性指标，数学上界为 0） | ⚠️ **是**：揭露 **P2 覆盖率 100% 含 22.4pp 注水**，诚实口径 77.6% **低于 85% 门槛** |
| `exp/source` | type 特征口径差是否污染 §6.2 的 +4.5pp？ | ❌ **否**（§6.2 两侧 type 同为 1.0，未受污染）；增益**反而增大**到 +6.4pp | ⚠️ **是**：`evaluate_fullcorpus` 的 A7/H5 从"证伪"翻转为"成立"（+0.3pp → +9.5pp）；A6 旧口径低估 1.8pp |

---

## exp/dynamics：源项 / 谱性质（已复核）

**假设（我提出）**：`hgnn.py` 的 `M=½(I+S)` 无源项，迭代收敛到连通分量平稳分布，
使 `metaphor_coherence` 退化为同分量指示器；这解释了 §6.3 中 GRU 与 HGNN 打平
（1.000 vs 0.950），即"聚合方式次要"可能是算子退化的产物。

**裁决：假设证伪。** 两条独立证据：

### 证据一：退化只在渐近意义成立，H4 工作在 layers=2

我原始测量取 **500 轮**（收敛值），而 H4 实际工作在 `layers=2`。逐层实测
（doc_project，无源项）：

| layers | 同分量 cos | 跨分量 cos | 与 X0 余弦（源保留） |
|---|---|---|---|
| 1 | 0.188 | 0.046 | 0.961 |
| **2（H4 工作点）** | **0.388** | **0.076** | **0.840** |
| 5 | 0.660 | 0.124 | 0.547 |
| 50 | 0.964 | 0.129 | 0.331 |
| 500 | **1.000** | 0.133 | 0.314 |

`layers=2` 时同分量余弦仅 0.388，距 1.0 很远，源信息保留 0.840。**"退化"不能
用来解释 layers=2 的 H4 数字。**

### 证据二：完美同分量指示器达不到 0.950（决定性）

实测该配对样本上：正样本 20（全部同分量）、负样本 752（其中 150 同分量）。
**完美同分量指示器的准确率上限 = (20 + 602) / 772 = 0.8057**，**低于** H4 实测的
0.950。这一条单独就足以排除退化解释。

### 结论稳健性

在 α ∈ {0.1…1.0} × ε ∈ {0, 0.1, 0.2} 共 18 个配置下，`|acc_flat − acc_hgnn| ≤ 0.05`
（至多一个样本对之差）。换成高功效口径（20 正 / 752 负穷举，ROC-AUC）后
**|ΔAUC| ≤ 0.005，95% CI 跨 0**，方向甚至轻微偏向 flat。

**"信号来自 L1 n 元共现、而非层级"的结论保持不变。** 源项没有打破平局。

### 新增负面结果（2 项）

1. **`raw = 0.150` 是口径产物，不是"反向判别"。** 默认哈希编码器下抽象域标签
   几乎无共享 n-gram，实测**全体并列率 94.6%**（正样本 85.0%、负样本 94.8% 余弦
   恰为 0），而配对准确率把并列计为错误。raw 的真实 ROC-AUC = **0.5500**，
   即**无信息**，而非有害。原表述应修正。
2. **n=20 的 H4 配对准确率统计功效不足。** 其"结论字符串"会随 α 抖动翻转
   （α=0.5 时打印"依赖跨层传播 ✅"），而同期 AUC 完全稳定。**建议把 H4 主指标
   换成全量负样本 AUC。**

### 已实现（保持向后兼容）

`hgnn.py` 新增 `alpha`（源项系数，默认 1.0）与 `leak`（阻尼 ε，默认 0.0）；
`α=1.0, ε=0.0` 与历史实现**逐位相同**（`maxdiff = 0.000e+00`，4/4 组合）。
新增 `propagation_matrix()` 供谱分析。`evaluate_hgnn.py` 增加 `--alpha/--leak`
与两行新条目。测试 **116 → 129 全绿**。

源项的真实价值：让**深层迭代**（layers ≥ 50）不再坍缩，而非改善 layers=2。

---

## exp/omega：查询侧可观测泛函 Ω（已复核）

**假设（我提出）**：实现查询侧 Ω，检验"低 Ω 预测触发词级联通路的 0.042 失效"。

**裁决：分离现象真实，但 Ω 无额外信息量。** 逐条：

| 命题 | 裁决 | 关键数字 |
|---|---|---|
| Ω 只读查询（对候选池不变） | ✅ 成立 | 632 查询 × 4 种候选池扰动 → **0/632** 变化 |
| 改写型 Ω 系统性低于重叠型 | ⚠️ 成立但效应中等 | AUC=0.349、Cliff's δ=−0.301、Cohen's d=−0.502 |
| 低 Ω 预测级联通路失败 | ❌ **近乎同义反复** | 全样本 AUC=0.941；**去掉 Ω=0 的 518 条后 AUC=0.589（CI 含 0.5，p=0.163）→ 无区分力** |
| Ω 比"触发词命中数"更有信息 | ❌ 否 | Spearman **ρ(Ω, n_seed)=0.9949**；Ω>0 ⟺ n_seed≥1 **632/632 一致** |
| Ω 门控优于现有判据 | ❌ 严格更弱 | 漏掉 66 条"有触发词但通路仍空"的查询 |
| Ω 三分量都有用 | ❌ **Ω_N 是死分量** | 见下 |

### 根因：级联构造使 Ω_N 结构性恒空（已独立复核）

生产本体 758 个级联中，**752 个（99.2%）成员框架共享同一目标域**（实测确认）。
原因是 `builder._ensure_cascades` 按**目标域**打包孤儿框架——按目标域分组，
自然不会有"沿级联涌现出新的目标域"。**级联规模中位数 = 1**。

后果：Ω_N 恒为空集，几何平均下 Ω 被迫由 ε 下界决定，于是 Ω 退化成
"查询有没有命中任何触发词"的 1-bit 指示器。而该指示器与"级联通路是否为空"
在代码结构上是**定理等价**。

### 这条负面结果的正面价值

它**定位了一个真实缺陷**：级联是检索组织层（论文 §6.3 结论"层级的价值应表述为
检索组织与可解释性"），但当前构造下 **99.2% 的级联没有跨目标域的横向组织能力**，
中位规模为 1，实际退化为"框架别名"。这比"Ω 不好用"更重要——它指向
**级联本体构造规则需要修订**（见第二代候选）。

### 一句话

Ω **可以被计算**（"能测"成立），其低值与级联通路失败**确实共现**；但在本基底上
Ω 相对"有没有命中触发词"这个 1-bit 指示器**几乎没有额外信息量**。分离现象是
"本体触发词词表覆盖不足"的投影，**不是**"改写破坏了隐喻连贯性"的度量。

---

## exp/degrade：降级来源封顶可靠度通道（已复核）

**假设（我提出）**：不丢弃 `F_LLM_*` 降级候选，但给其封顶可靠度，能否让类型/结构
约束终于影响 P1 而不损失召回？

**裁决：假设不成立，且是结构性不可能——但揭露了一个更重要的指标问题。**

### 假设为何不可能成立（机制性）

**P1 是句级布尔指标**（`pred = bool(edges)`），而软可靠度通道只作用于打分/排序，
不改变任何句子的产出与否。**数学上界就是 0**，不是调参问题。

**更关键的是：误判根本不来自降级边。** 实测全部 **7/7 条 FP（字面误判）句都由
本体正式登记的框架支撑，0/7 是仅由降级框架支撑**。句级判别 AUC = **0.3913**
（即反相关：字面句 100% 可靠度 1.0，而 21.7% 的真隐喻句可靠度 0.5）。因此任何
折扣档位（floor 1.0/0.9/0.75/0.5）都不优于基线。**"惩罚降级来源"与"过滤字面误判"
在这批数据上完全不相交。**

实测：召回 0.696 → 0.696（逐位不变，通道实现正确、召回完全保住）；但排序被**损害**：
人工加权 MRR@10 **0.9948 → 0.8922（−0.1025）**，原因是 by-construction 金标与通道
前提直接冲突（26.6% 的金标 chunk 含降级边，降权即惩罚金标自身）。训练路径
+0.0000（clue AUC=0.999，金标已在 99.5% 查询中排第一，指标饱和），
**人工金标检索集（唯一不与通道前提冲突的尺子）效应恰为 ±0.0000**。
floor 全档位扫描无一档优于基线。**净增益为零。**

**张力未被解决。** 参考系统的封顶可靠性模式在本项目**不适用**，因为本项目 P1 不是
排序指标而是**存在性布尔指标**——这是与参考系统场景的关键结构差异，也是本实验最有
价值的发现。

### 但揭露了 P2 覆盖率的注水（已独立复核）

| 口径 | 值 |
|---|---|
| 现报 `hierarchy_coverage` | **1.000** |
| **诚实口径：本体登记框架 + 本体登记级联** | **0.776** |
| 退化边占比 | 22.4% |
| 级联总数 / 其中 `C_ADHOC_` 事后补的 | 408 / **184（45.1%）** |

独立复核（重跑 `probe_provenance2.py`）：超边 883 条，报出覆盖率 1.000，
**框架在本体中登记的边 = 685/883 = 0.776**，级联 408 其中 C_ADHOC 184。数字吻合。

**含义**：现报的 100% 覆盖率有 22.4pp 由"抽取器临时回退框架 + `_ensure_cascades`
按目标域事后补的 `C_ADHOC_*` 级联"撑起。排除后**本体正式归属仅 77.6%，低于 P2
的 85% 门槛**。论文附录 B 的 P2 状态需从 ✅ 改为**条件性达标**（现报口径 100%、
诚实口径 77.6%），并在限制章节说明两个口径。

### 实现过程中的一个关键坑（值得记录）

**判据必须是"本体有无条目"，不是 `F_LLM_` 前缀。** `llm_ontology.py:201` 用
`_stable_id("F_LLM", s, t)` 给**自举沉淀的正式框架**命名，于是生产本体的框架
**2177/2209 = 98.6% 以 `F_LLM_` 开头**（已独立验证：抽查的 `F_LLM_*` 均
`get_frame() is not None`）。**按前缀判定会把整个生产本体误判为"退化"**，
通道退化成"惩罚一切"。正确判据只有 `ontology.get_frame(frame_id) is not None`。
测试 `test_judgement_is_registration_not_prefix` 把这个坑钉死。

### 已实现（保持向后兼容）

新增 `metaphor_graph/provenance.py`（可靠性派生、分级标签、有下界乘性折扣）；
`models.py` 增加 `provenance_reliability: float = 1.0`（默认值保证旧数据不受影响）；
`extended.py` 扩展边取链上 `min(...)`（**退化边不得借合并洗白**）；
`retrieval.py` 两条打分路径都乘折扣。设计选择"**有下界的乘性折扣**"而非加性 bonus
（不改变相对次序）或硬过滤（丢候选、召回崩），有论证。

`health.py` 新增诚实覆盖率接口：**不传 `ontology` 时字段为 `None`，历史口径逐位不变。**

### 一个口径陷阱（已在报告中记录）

直接跑 `evaluate_real.py --use-llm` 在无 API key 时会回落到 `LocalHeuristicBackend`，
而它不实现 `discover_batch`/`batch_refine`，于是 `--llm-cache` **根本没被消费**，
产出**假性 P1 = 0.434**。正确做法是用 `PrecomputedBackend` 喂真实缓存重放
（P1 = 0.092，与 README 记录一致）。这与 README §7.1 记录的是同一类陷阱。

---

## exp/source：type 特征口径一致性（已复核）

**假设（我提出）**：`training.py` 与 `retrieval.py` 对 `F_LLM_*` 降级边的 `type`
特征给出 1.0 vs 0.0，§6.2 的 +4.5pp 训练增益可能部分来自此口径差。

**裁决：bug 真实存在，但我的因果链推断错了。§6.2 未被污染，增益反而增大。**

### bug 确认（比我原先描述的更精确）

两条路径确实不一致，但**分歧面比我原先说的窄得多**：

| 路径 | 表达式 |
|---|---|
| `training.py:81/113` | `type_ok = 1.0 if cand.frame_id else 0.0` |
| `retrieval.py:250-252` | `get_frame()` 对未注册 `F_LLM_*` 返回 `None` → `mtype` 退化为原始 id → `type_valid` = False → **0.0** |

关键修正：`F_LLM_` 前缀被**两个互不相交的 id 空间**共用——

- `extractor.py:113`：`md5("src|tgt")`
- `llm_ontology.py:201`：`_stable_id` 用 `md5("src||tgt")`（**双竖线**）

实测（856 条 L1 边）：**90.5% 的边以 `F_LLM_` 开头，但只有 188 条（22.0%）真正分歧**
——587 条回退边其实**已注册**（两条路径都给 1.0），只有 188 条未注册的才分歧。

### 我的因果链错在哪

**§6.2 的 "manual weighting" 根本不调用 `metaphor_retriever_score`。** 它走
`evaluate_retrieval.score_conditions`，用**共享的 7 维特征**，其中 `hand_weighted`
= `0.35·sem + 0.25·struct + 0.20·clue + 0.20·type`。而该候选池中
**`type` 恒为 1.0（已独立复核：25/25 边都有 frame_id）**——是**死维度**，
被偏置吸收。因此 §6.2 两侧 `type` 都是 1.0，**从未受口径差影响**。

> 教训：我依据「两处表达式不同」直接推断「已上报数字被污染」，未先确认
> **被上报的那条路径实际调用的是哪段代码**。这是本代实验里我自己犯的推理错误。

### 修复

新增 `ontology.type_reliability` / `type_reliability_of` 作为**唯一真源**：
**1.0** 已注册 / **0.5** 未注册回退（**封顶而非丢弃**，保住 +55.8pp 召回）/
**0.0** 无框架或已注册但非法。三处调用点统一，本体实例贯穿 `_pair_features` 与 `_add`。

### §6.2 重测：增益增大而非消失

| 配置 | 修改前 | 修改后 |
|---|---|---|
| 人工加权 | 0.502 | **0.481** |
| 自监督 | 0.547 | **0.545** |
| 弱监督 | 0.536 | **0.526** |
| **训练增益** | **+4.5pp** | **+6.4pp** |

真实句向量行逐位复现（0.707/0.759/0.756 vs 报告 0.707/0.758/0.755，缓存命中 100%），
修复后为 0.673/0.751/0.734，增益 **+5.2pp → +7.8pp**。

**机制**：`type` 的单特征 AUC ≈ 0.497（接近噪声）；训练器学到的权重 **+0.049 ≈ 0**，
而人工加权**硬编码 0.20**。修复把一个"无害常数"变成了"有害噪声"，因此
**人工加权受损更重**，训练的相对优势反而扩大。

### 结论翻转（需人工复核）

`evaluate_fullcorpus` 的 **A7/H5 从"+0.3pp，H5 证伪"翻转为"+9.5pp，H5 成立"**。
这一条影响 §6.2 与 §7 的结论表述，**建议人工复核后再采纳**。

### A1/H1 不变

日志逐字节相同：P1 0.092 → 0.092，枚举拦 0 + 连贯性拦 24。修复**有意不触碰**此处
——封顶到 0.5 而非 0.0，正是保住召回的原因。

### 其他口径分歧（已排查）

`same_frame` 0.0%、`same_cascade` 0.08%（max 聚合残差）、`struct` 0.0% —— 均一致。
`ground_jaccard` **4.76% 因定义不同**（包含比 vs 真 Jaccard）—— 已标注，未改动。
`sem` 的 clamp 不一致为潜在问题（此数据上余弦非负，未触发）。

### 诚实裁决

**报告的 +4.5pp 不是假象**——§6.2 两条路径本就一致，修复后增益反而增大到 +6.4pp。
真正发现的问题是：`type` 是死维度；且 `retrieval.py:250` 使 **A6 被低估 1.8pp**
（旧训练编码下 0.783 vs 0.801）。

## 第二代：exp/cascade —— 级联构造修订（已复核）

**假设（由 omega 的根因发现导出）**：当前 `_ensure_cascades` 按目标域打包，
使级联退化为框架别名；换成按源域 / 共现喻底 / MetaNet 成员关系打包，
应恢复 L3 的组织能力，并可能移动检索指标。

**裁决：缺陷完全复现，但【度量中性】—— 换规则不改变任何主指标。**

### 缺陷复现（比第一代更完整）

同一"按目标域分组"规则被**实现了四遍**：`llm_ontology.py:197-233`、
`ontology_clean.py:145-153`（`rebuild_cascades`）、`builder.py:167-170` + `:173`
（`_ensure_cascades`）。生产本体 758 个级联中：

- **752（99.2%）** 成员共享单一目标域
- 规模中位 **1**、均值 2.91、最大 55；**412（54.4%）为单例**
- 实测后果：**96.6% 的边经级联展开后得不到任何新目标域**（均值 +0.037）

### 决定性证据：全单例对照

构造一个合成对照——**每条边给唯一 `cascade_id`**（结构上最退化，但非空）：

| 配置 | 人工加权 MRR@10 | Hits@3 |
|---|---|---|
| 生产现状（本体级联 + 兜底） | 0.5025 | 0.6044 |
| 按源域 | 0.5025 | 0.6044 |
| 全单例（最退化对照） | **0.5025** | 0.6044 |
| **L3 全消融（cascade_id 全空）** | **0.4681** | 0.5552 |

**三种构造规则（含最退化的）给出完全相同的 MRR（0.5025）**，而彻底移除 L3
才降到 0.4681（**−0.034**）。结论：**L3 的唯一因果通道是 `cascade_id` 的
"存在性"，不是它的结构**——经 `struct = 0.5·len(ground) + 0.5·[cascade_id 非空]`
（`retrieval.py:73`）。级联结构本身对检索**零贡献**。

跨规则不变量：A6 **0.783/0.095 逐位相同**、H4 **0.960**、A5/A7/A9/H3 逐字一致。
唯一对规则敏感的是级联通路 Recall@10（0.042 → 0.076 源域 / 0.131 source_type），
**仍远低于语义通路的 1.000**，故 §6.1"级联通路在改写查询上脆弱"**成立**。

### 新增：`same_cascade` 与 `same_frame` 高度冗余

| 构造 | same_cascade 命中 | 其中同框架占比 | 增量信息 |
|---|---|---|---|
| 生产（目标域） | 29 | **0.517** | 0.483 |
| 按源域 | 76 | **0.197** | 0.803 |
| 全单例 | 15 | **1.000** | **0.000** |

**当前构造下 `same_cascade` 有 51.7% 与 `same_frame` 重复**（全单例时 100% 重复，
即完全冗余）。这为 §6.2"训练排序器增益取决于查询分布"提供了一个机制侧注脚。

### Ω_N 复活但 Ω 未复活

Ω_N>0 占比 **1.4% → 14.8%**（源域）/ 18.0%（source_type）。但 Ω 仍是 1-bit 指示器：
ρ(Ω, n_seed) = **0.9950**，"Ω>0 ⟺ n_seed≥1"一致率 **1.0000**。

**但发现一个有价值的新信号**：原始涌现计数 `n_emergent` **确实带信号**——
按 n_seed 分层后的条件 AUC **0.836 [0.759, 0.909]**，显著优于 Ω 的
**0.654 [0.551, 0.750]**（CI 不重叠），且 ρ(n_emergent, n_seed)=0.103（p=0.275，
非代理）。**Ω 的几何平均 + ε 下界 + 完备度合成把该信号稀释掉了。**
（限定：n=114 子集；且它预测的是弱中间量——级联通路 Recall ≤0.131。）

### 覆盖率归因修正（与 `degrade` 一致，更精确）

报出的 **100% L2/L3 覆盖率由 `builder._ensure_cascades` 的目标域兜底产生，
不是本体级联查表的结果**。兜底关闭后覆盖率 = **0.767，低于 0.85 门槛**。
两种方式都是查表式（零 LLM 调用），故**成本主张（~20 vs ~187 次/千边）不受影响**，
但摘要与 §5.2 的措辞应改为"级联查表 **+ 目标域兜底**"。

### 是否应改默认：否

主通路对规则免疫；且改本体级联会重写 **733 个稳定 ID**（缓存键、导出、Neo4j、
双语对齐）。若确需运行时组织力，可用 `orphan_cascade_rule="source_type"`——
一行、零风险、不动本体、覆盖率保持 1.000，ad-hoc 级联从 size≡1 变为中位 2 / 最大 6。

### 顺带修掉一个真实 bug

`mipvu.py:91` 把整条 MIPVU 通道**门控在 6 个硬编码级联 ID** 上。任何替换级联
构造规则的实验都会**静默关掉该抽取通道**（丢 7 条 L1 边，856→849），
从而混淆两个变量。已改为"框架存在 + 框架有级联归属"；默认路径逐位不变（856 边）。
测试 **129 → 154 全绿**（含一项把该缺陷本身钉成回归护栏）。

---

## 第三代：exp/typefeat —— 特征审计与**评测基准审计**（已复核）

**假设（由 `source` 的发现导出）**：`type` 是死维（单特征 AUC 0.497），应处置；
顺带审计全部 7 维特征。

**裁决：`type` 不是死维（gen1 口径误读），但真正的问题是【评测基准本身不可用】。**

### 7 维特征全量审计（首次完成）

| 特征 | 配对 AUC | 学到权重 | 人工权重 | 裁决 |
|---|---|---|---|---|
| `sem` | **0.6277** [0.6023,0.6522] | +0.6911 | 0.35 | **唯一有真实判别力** |
| `struct` | 0.5197 | +0.0195 | **0.25** | 弱；**人工过权 49 倍**（前两代漏掉） |
| `clue` | 0.5087 | +1.2543 | 0.20 | 弱（评测口径；训练集口径 0.9985 系饱和） |
| `type` | 0.5249 | +0.0487 | 0.20 | 弱正向（**非死维**，见下） |
| `same_frame` | 0.5053 | +0.1953 | 0.00 | 接近随机 |
| `same_cascade` | 0.5070 | +0.1958 | 0.00 | 接近随机 |
| `ground_jaccard` | 0.5300 | +0.4965 | 0.00 | 弱 |

**无任何维度达到 `dead`（CI 覆盖 0.5 且 \|AUC−0.5\|<0.02）或 `harmful` 判定。**

### 纠正 gen1 的口径误读

gen1 报 `type` AUC **0.4973**，该数字来自 `feature_auc(ds)`，其中 `ds` 是
**自监督训练集**，**不是评测候选对**。评测金标上 `type` 的配对 AUC =
**0.5249 [0.5092, 0.5401]**（CI 不含 0.5）。三个 gen1 数字（0.4973 / +0.0487 / 0.20）
**全部逐位复现**，但口径标签需修正：`type` 是**弱维且与 `struct` 共线**
（二值重叠 1.000、秩相关 +0.836）。

### 处置实验：全部为 null

| 处置 | Δ MRR | 95% CI | 显著？ |
|---|---|---|---|
| 删除 `type`（重训 6 维） | +0.0028 | [−0.0043, +0.0097] | 否 |
| 删除 `same_cascade` | +0.0017 | [−0.0034, +0.0065] | 否 |
| 6 个替代信号（frame_support / tier / cascade_size / n_emergent / registered / ground_len） | 最大 +0.0082 | **全部覆盖 0** | 否 |

### 真正的发现：评测基准存在构造锚定

| 检验 | 实测 |
|---|---|
| LLM 金标包含「产出该问题的 chunk」 | **632/632 = 100%** |
| LLM 金标**恰好只等于**该 chunk | **619/632 = 97.9%** |
| 可真正检验非构造检索的查询 | **仅 13/632 = 2.1%** |

论文 §4 称该金标为"**非构造金标**"——**该标签不成立**。LLM 只做过滤（筛掉不相关
候选），**没有解除锚定**。旁证：候选池 min=2 / median=7–8 / max=10，
**池 ≤10 的查询占 100%** → **Hits@10 平凡恒为 1.000**（已独立复核）。

**MRR 来源分解（决定性）**：

| 配置 | MRR（产出 chunk 列，n=632） | MRR（非产出列，n=13） |
|---|---|---|
| 人工加权 | 0.4728 | 0.6987 |
| 训练后 | 0.5372 | 0.6923 |
| 差值 | **+6.44pp** | **−0.64pp** |

**训练增益全部落在"把构造锚点排更前"这一列**，非产出列为负。

### 训练增益的重新归因

**仅把人工权重改成与学到权重同比例**（不训练任何模型）：

| 配置 | MRR@10 |
|---|---|
| 人工加权 M0（原权重 0.35/0.25/0.20/0.20） | 0.4809 |
| **人工加权 M3（按学到比例）** | **0.5496（+6.87pp，p=0.0000）** |
| 训练后 7 维 T7 | 0.5447 |

**人工权重校正后反超训练后排序器。** 训练增益降为 **+0.49pp [−0.89, +1.84]
（CI 覆盖 0）**；**1 维 `sem` 单信号 = 0.5474，与 7 维训练后无差异**（Δ=+0.0025，
CI 覆盖 0），且在**真实句向量下纯 `sem` 显著优于训练后 7 维（+1.76pp，p=0.003）**。

**归因**：已上报的"训练增益"**主要是人工权重配错的产物**（`struct` 过权 49 倍、
`type` 19 倍），而非训练学到了更优排序函数。

### gen1 的 A7/H5 翻转被完全解释

gen1 报告 fullcorpus 的 A7/H5 从"+0.3pp 证伪"翻转为"+9.5pp 成立"。本代实测：
**+9.18pp 全部来自「人工硬编码 0.20」这一项**；训练侧去 `type` 的效应
**恰为 +0.00pp**。真实增量 **+0.29pp**，而非 +9.5pp。

---

第一代四个方向 + 第二代一个方向全部返回，**五个假设全部证伪或需修正**
（其中三个是我提出的，我的因果推断错了两处）。但净产出显著为正：

**改变了已上报结论的（3 条）**：
1. **P2 覆盖率 100% 含 22.4pp 注水**，诚实口径 77.6%（`degrade` 与 `cascade` 独立确认；
   `cascade` 进一步定位为"目标域兜底产生，非本体查表"）
2. **A7/H5 从证伪翻转为成立**（+0.3pp → +9.5pp），待人工复核（`source`）
3. **L3 级联层对检索零结构贡献**（`cascade`）。全单例对照与生产配置 MRR 完全相同
   （0.5025），只有彻底移除才降到 0.4681。级联的唯一作用是 `cascade_id` 非空
   这一点被 `struct` 特征读到。**§6.3"层级的价值在检索组织与可解释性"需附加条件**。

**新增负面结果（4 条，建议写入 §7）**：
- Ω 无额外信息量（ρ=0.9949，退化为 1-bit 触发词指示器）
- 封顶可靠度通道对 P1 效应恒为 0（且 7/7 条误判全由正式框架支撑）
- HGNN 加源项不打破 flat/HGNN 平局（|ΔAUC| ≤ 0.005）
- 更换级联构造规则不移动任何主指标（度量中性）

**方法学修正（2 条）**：
- H4 主指标应换成全量负样本 AUC（n=20 功效不足）
- `raw = 0.150` 应表述为"无信息（AUC 0.550）"而非"远低于随机"

**加强的既有结论（1 条）**：
- "信号来自 L1 n 元共现、层级非必要"在高功效口径下依然成立（|ΔAUC| ≤ 0.005）

**定位的真实缺陷（4 条）**：
- 级联构造按目标域打包 → 99.2% 单目标域、54.4% 单例、中位规模 1（同一规则实现四遍）
- `type` 是死维度（单特征 AUC 0.497）
- `same_cascade` 与 `same_frame` 冗余 51.7%（当前构造下）
- `mipvu.py` 硬编码 6 个级联 ID 门控整条通道（已修）

**找到的新信号（1 条，值得跟进）**：
- 原始涌现计数 `n_emergent` 带真实信号：分层条件 AUC **0.836 [0.759, 0.909]**
  vs Ω 的 0.654 [0.551, 0.750]（CI 不重叠），ρ(n_emergent, n_seed)=0.103。
  **Ω 的几何平均 + ε 下界 + 完备度合成把该信号稀释掉了**——这说明"合成方式"
  本身是需要检验的设计选择，而非中性的工程封装。

---

## 总结：三代七个方向

第一代四方向 + 第二代一方向 + 第三代两方向全部返回，**七个假设全部证伪或需修正**
（其中四个是我提出的，我的因果推断错了三处）。但净产出显著为正：

**改变了已上报结论的（7 条）**：
1. **评测基准存在构造锚定**（`typefeat`，**最重要**）。632/632 = 100% 的"非构造
   金标"都包含产出该问题的 chunk，97.9% 只包含它；候选池 median 7–8 / max 10
   → Hits@10 平凡恒为 1.0。训练增益 +6.44pp 全部落在"产出 chunk"列。
2. **训练增益主要是人工权重错配的产物**（`typefeat` + `source`）。人工权重按学到
   比例校正后达 **0.5496，反超**训练后 0.5447；训练增益降为 +0.49pp（CI 覆盖 0）；
   1 维 `sem` 即等于 7 维训练后（真实句向量下显著更优）。
3. **P2 覆盖率 100% 含 22.4pp 注水**（`degrade` + `cascade` 独立确认），
   诚实口径 77.6% 低于 85% 门槛；`cascade` 进一步定位为"目标域兜底产生"。
4. **L3 级联层对检索零结构贡献**（`cascade`）。全单例对照与生产配置 MRR 完全相同。
5. **A7/H5 的"+9.5pp 翻转"归因错误**（`typefeat`）：+9.18pp 全来自人工硬编码
   0.20，训练侧效应恰为 +0.00pp，真实增量 +0.29pp。
6. **`type` 不是死维**（`typefeat`）：gen1 的 0.497 是训练集口径，评测配对
   AUC = 0.5249（CI 不含 0.5）。
7. **`raw = 0.150` 应表述为"无信息（AUC 0.550）"**（`dynamics`）。

**新增负面结果（5 条，建议写入 §7）**：
- Ω 无额外信息量（ρ=0.9949，退化为 1-bit 触发词指示器）
- 封顶可靠度通道对 P1 效应恒为 0（7/7 条误判全由正式框架支撑）
- HGNN 加源项不打破 flat/HGNN 平局（|ΔAUC| ≤ 0.005）
- 更换级联构造规则不移动任何主指标（度量中性）
- 7 维特征的全部处置（删除/替换）均无显著效应（CI 全部覆盖 0）

**方法学修正（3 条）**：
- H4 主指标应换成全量负样本 AUC（n=20 功效不足）
- 评测口径需先审计（构造锚定、平凡饱和、人工权重错配、口径不一致）
- **主控必须独立复核子方向结论**——第一代据此发现两处推断错误

**加强的既有结论（1 条）**：
- "信号来自 L1 n 元共现、层级非必要"在高功效口径下依然成立（|ΔAUC| ≤ 0.005）

**定位的真实缺陷（5 条）**：
- 级联构造按目标域打包 → 99.2% 单目标域、54.4% 单例（同一规则实现四遍）
- `struct` 人工权重过权 49 倍、`type` 19 倍
- `same_cascade` 与 `same_frame` 冗余 51.7%–64%
- `mipvu.py` 硬编码 6 个级联 ID 门控整条通道（已修）
- `evaluate_real.py` 无 key 时静默回落 → 假性 P1 = 0.434

**未决（`emergent` 进行中）**：
- `n_emergent` 的 0.836 分层 AUC 能否经受"集合大小"控制？初步结果显示
  多数查询侧标量在控制集合大小后回落到 0.5（见其 exp_g3_5 输出）。

---

## 第四代候选（待 `emergent` 返回后定稿）

已完成：`exp/cascade`、`exp/typefeat`。

**核心待办已从"改进系统"转为"修复评测"**：
1. **重建检索基准**：解除构造锚定（查询不再由边生成，或金标排除产出 chunk），
   扩大候选池使 Hits@10 脱离平凡饱和。这是所有检索结论的前提。
2. **A7/H5 与 §6.2 表述落地**（已有具体建议见 `论文修正建议.md` 修正 9/10）。
3. **P2 覆盖率口径落地**（修正 1）。
4. **`n_emergent` 结论收口**（待 `emergent`）。

