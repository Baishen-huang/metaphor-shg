# -*- coding: utf-8 -*-
"""P3 跨 chunk 消歧 + A7/A9 检索消融 —— 带金标评测语料。

设计原则（诚实可复现，零标注成本）：
- 全部使用离线默认本体（MetaNet + 自举）覆盖的**常规隐喻**，不依赖 LLM 开放发现，
  因此评测完全离线、确定性、零 API 费用。
- 金标（GOLD_EXTENDED / GOLD_RETRIEVAL）是「人工判定的正确答案」，
  与 `link_extended_metaphors` / `RetrievalEngine` 的内部实现无关，
  评测只比较「系统输出 vs 金标」。
- 语料刻意制造：多条跨 chunk 扩展隐喻（同框架 + 同源域 + 喻底交集 + 间距<3）、
  字面干扰句、以及只能用隐喻通路召回的跨域查询。

运行：python -m metaphor_graph.evaluate_retrieval
"""

# ---------------------------------------------------------------------------
# 评测文档（每篇是 chunk 列表；chunk 索引即其在列表中的位置）
# ---------------------------------------------------------------------------
DOCS: dict = {
    "doc_project": [
        # 0 跨域锚点：字面"推进不动"实际写"泥潭"（地形/障碍，孤立）
        "我们的项目现在陷在泥潭里，每前进一步都要拔出腿来，整个团队都快窒息了。",
        # 1-2 机器扩展隐喻（发条/拧紧 → 拧紧/机械）
        "别活得像根发条，每天被拧紧了才肯动弹。",
        "别每天拧紧自己，机械重复的日子里早就没有弹性了。",
        # 3-4 战争(商业)扩展隐喻（抢滩/护城河 → 阵地/突围，单一 COMP_WAR 框架）
        "这场商战里，谁先抢滩谁就守住了护城河，落后的一方只能出局。",
        "对手在战场上缴获了我们的阵地，最后一轮突围也宣告失败。",
        # 5-6 热-愤怒扩展隐喻（怒火翻腾 → 爆发灼烧，单一 ANGER_HEAT 框架，不含"积压"以免串入容器）
        "看到这个结果我火大，胸口的怒火一直在翻腾。",
        "那股怒火彻底爆发了，灼烧感顺着脊背往上窜。",
        # 7-8 容器-情绪扩展隐喻（积压 → 满溢装不下）
        "她把委屈都积压在心里，表面还要装作无事发生。",
        "情绪终于满溢出来，再多一点就彻底装不下了。",
        # 9 字面干扰：不应判为隐喻，也不应参与任何扩展
        "苹果发布了新手机，屏幕比上一代更大，续航也更长。",
        # 10 负样本：同样是"战争-商业"框架，但与 c3/c4 间距≥3，
        #     正确行为应「不合并」（验证距离约束拦住跨距误合并）
        "多年后另一场商战打响，新的护城河又被筑了起来。",
    ],
    "doc_relationship": [
        # 0 容器-情绪（心里装不下）
        "这段关系里他始终关着门，把委屈全积压在心里。",
        # 1 容器-情绪扩展（满溢）
        "等到情绪满溢出来，连一句解释都显得多余。",
        # 2 机器扩展（被拧紧）
        "他像根发条，被人拧紧了才愿意为这段关系动一下。",
        # 3 机器扩展（机械重复）
        "纪念日变成机械重复的流程，早就没有了温度。",
        # 4 战争(争论)扩展（战壕/紧逼/败退 → F_ARG_WAR）
        "每次争吵都变成拉锯战，他困在战壕里步步紧逼，我节节败退。",
        # 5 战争(争论)扩展（攻防/溃败，与 c4 同一 ARG_WAR 框架）
        "我守不住自己的阵地，最后那次攻防也溃败了。",
        # 6 字面干扰
        "这家餐厅新出了春日限定套餐，摆盘很精致。",
    ],
}

# ---------------------------------------------------------------------------
# 金标：跨 chunk 扩展隐喻（每条 = 应被合并为一条扩展超边的 chunk 索引集合）
# 数值来自人工判定；与 link_extended_metaphors 实现无关。
# ---------------------------------------------------------------------------
GOLD_EXTENDED: dict = {
    "doc_project": [
        {1, 2},        # 机器：发条/拧紧 ↔ 拧紧/机械
        {3, 4},        # 战争-商业：抢滩/护城河 ↔ 阵地/突围
        {5, 6},        # 热-愤怒：积压怒火 ↔ 爆发灼烧
        {7, 8},        # 容器-情绪：积压 ↔ 满溢装不下
    ],
    "doc_relationship": [
        {0, 1},        # 容器-情绪：积压 ↔ 满溢
        {2, 3},        # 机器：发条 ↔ 机械重复
        {4, 5},        # 战争-争论：攻防 ↔ 阵地/突围
    ],
}

# ---------------------------------------------------------------------------
# 金标：跨域检索查询 → 相关 chunk 索引集合
# 这些查询字面不含答案词，只能经由隐喻通路（级联/框架）召回。
# ---------------------------------------------------------------------------
GOLD_RETRIEVAL: list = [
    ("doc_project", "为什么我们的项目推进不动、寸步难行？", {0}),
    ("doc_project", "他为什么活得像被上了发条，一点弹性都没有？", {1, 2}),
    ("doc_project", "在商业竞争里谁先占了先机、守住了壁垒？", {3, 4}),
    ("doc_project", "他为什么情绪失控、怒火一下子全爆发了？", {5, 6}),
    ("doc_project", "她为什么心里的事越来越多，最后装不下了？", {7, 8}),
    ("doc_relationship", "这段关系里他为什么总把情绪憋在心里？", {0, 1}),
    ("doc_relationship", "他为什么在这段关系里像个被上紧的机器？", {2, 3}),
    ("doc_relationship", "每次吵架他为什么总能压我一头、让我节节败退？", {4, 5}),
]

# 评测门槛（对齐方案 / README §7.0）
THRESHOLD_P3_ACC = 0.70      # 跨 chunk 消歧准确率（F1）> 70%
