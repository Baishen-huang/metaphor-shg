# 后续待办（详细版）
> ## ⚠️ 口径更新（2026-09-25）
> 本文档中的**检索类数字**建立在已被证伪的评测口径上：
> ① "LLM 非构造金标"**不成立**（100% 金标含产出该问题的 chunk）；
> ② 候选池 ≤10 使 **Hits@10 平凡饱和**、随机 MRR 期望即达 0.37；
> ③ 人工权重错配（struct 过权 37 倍）使"训练增益"失真。
>
> **修复后口径**（全局池 1,100 + 去锚定）：anchored MRR 0.8956 /
> deanchor 0.2330，随机 0.0069 —— 锚定抬高 ≈ +0.66，但去锚定仍为随机的 34 倍。
> 权威汇总见 `experiments/实验结论汇总.md`；论文修正见 `论文初稿.md` §4.1/§6.7。

> 更新：2026-08-31（基于论文初稿 v0.2）。目标会议：ACL/EMNLP/COLING 主会。
> 优先级：P0=投稿必需 / P1=强烈建议 / P2=加分 / P3=长期。
> 每项含：具体步骤 → 依赖 → 预估成本 → 验收标准。

---

## A. 论文与投稿线（P0）

### A1 英文翻译与 polish
- **步骤**：① 按 v0.2 章节逐节译英（摘要/引言先译，供导师/合作者审）；② 术语表
  统一（hyper-hypergraph / n-ary ground set / cascade / dual-stage pipeline /
  literal-false-positive rate）；③ 用 §附录 A 命令逐一核对英文稿每个数字。
- **依赖**：无（素材全齐）。**成本**：约 3–5 个工作日。
- **验收**：全文数字与 `效果对比.md` 一致；复现命令在干净环境跑通。

### A2 主表统一真实向量口径（二选一后定稿）
- **步骤**：① `evaluate_fullcorpus --embedder real`（已跑，0.998–0.999）；
  ② `evaluate_retrieval` 增加 `--embedder real` 并重跑 P3/H3/A5（2 文档诊疗集）；
  ③ 决策：主表用真实向量 + 哈希作"零依赖复现"附录，或维持现状双口径。
- **依赖**：智谱 key（已有）。**成本**：半天。
- **验收**：论文所有表同口径，或明确标注双口径；NgramEmbedder 转正决策一并定。

### A3 图表制作
- **步骤**：① 框架图（四层超超图 + 双段管线，draw.io/tikz）；② 结果图
  （帕累托前沿：规则→LLM；三通路 recall 柱状；A6 对照柱状；排序器条件化折线）；
  ③ 扩展链示例框图（cj11 战争链跨 chunk 实例）。
- **依赖**：无。**成本**：1–2 天。**验收**：图内数字与表一致。

### A4 金标人工抽查（投稿必需）
- **步骤**：① 从 632 条改写查询按分层抽样抽 50 条；② 2 名标注者独立判
  「LLM 金标 chunk 是否真相关」；③ 报告 Cohen's κ 与不一致样本修正。
- **依赖**：2 名标注者（每人的时间）。**成本**：每人 2–3 小时。
- **验收**：κ ≥ 0.7；论文限制章节更新为"人工抽查已做"。

### A5 相关工作补 2026 文献
- **步骤**：检索 HyperRAG/HyperGraphRAG/MetaphorBoost 之外的 2025–2026 新作
  （graph RAG 综述、metaphor LLM 检测），补引用与对比句。
- **成本**：半天。**验收**：相关工作覆盖到投稿月份。

---

## B. 方法迭代线（P1）

### B1 coherence / LLM 校验接入 L1.5 生产管线
- **步骤**：① `verify_chains_batch` 已存在于 llm_backend；把它挂为
  `MetaphorSHGBuilder(llm_verify_extended=True)` 的一等参数（当前只在评测脚本
  后过滤）；② builder 内部在 link_extended_metaphors 后调用；③ 默认值决策：
  `extended_continuity` 默认仍 ground1（密度优先）+ 校验开关默认开（精度）；
  ④ 重跑诊疗集 P3 F1（门槛 >70%）与语料密度/精度确认。
- **依赖**：智谱 key。**成本**：半天。**验收**：管线参数化完成、P3 门槛不破、
  语料精度复测 66.7% 级。

### B2 链校验样本扩充（统计强度）
- **步骤**：① 补判剩余 5 条（glm-4.7 配额恢复后自动增量，缓存支持）；
  ② 将 judge 扩到全部 52 条候选（双 judge：glm-4.7 + glm-5.3，报告一致性）；
  ③ 把 66.7% 的置信区间补上（Wilson 区间）。
- **依赖**：glm-4.7 免费档配额（429 墙解除）。**成本**：¥1 以内 + 1 小时。
- **验收**：全 52 条双 judge 判定 + 置信区间入论文。

### B3 排序器非构造金标扩展
- **步骤**：① 用 LLM 对 632 改写查询的全部候选对做相关性标注（与 A6 同一套
  批量基础设施，约 300k tok ≈ ¥1）；② 以该金标重训/重评排序器
  （self vs weak vs hand）；③ 报告真实 MRR 差与置信区间。
- **依赖**：智谱 key。**成本**：¥1–2 + 半天。
- **验收**：排序线闭环（当前 0.497/0.547/0.758 的差异在非构造金标下有检验）。

### B4 双段管线接入 RAG demo
- **步骤**：demo_rag 的语料从诊疗集扩到 106 文档语料；检索后自动走
  verify_chains_batch 过滤扩展链再打包上下文；与 packing 消融最优配置对齐。
- **依赖**：B1。**成本**：半天。**验收**：demo 在真实文档上端到端出答案。

---

## C. 评估线（P0/P1）

### C1 真人评估（方案 §6.4，投稿必需）
- **步骤**：① 招募 3 语言学 + 3 领域 + 5 普通用户；② 评测集 = 8 查询 × 2 系统
  （隐喻上下文/纯向量对照）匿名呈现（复用 llmasjudge 的随机化与缓存）；
  ③ 4 维度 Likert-7 + 排序一致性；④ 与 AI judge 结果对比写入论文
  （judge 敏感性发现的真人验证）。
- **依赖**：评估者时间。**成本**：组织半天 + 每人 30 分钟。
- **验收**：κ 报告 + 四维度均值表替换/补充 §6.5。

### C2 judge 交叉扩展
- **步骤**：接入第三家 judge（DeepSeek key 恢复或 OpenAI），对 packing 消融与
  llmgold 金标做三模型交叉；报告 judge 间一致性矩阵。
- **依赖**：任一非智谱 key。**成本**：¥2 以内。
- **验收**：三模型 judge 一致性表（强化 §6.5 的敏感性结论）。

### C3 A6 ConceptNet 原生复测
- **步骤**：api.conceptnet.io 恢复后（或镜像/离线子图），以
  `evaluate_a6.py` 同接口换装 ConceptNet 关联重跑。
- **依赖**：服务恢复。**成本**：1 小时。**验收**：三通路对比表加 ConceptNet 行。

---

## D. 工程与资源线（P1/P2）

### D1 Neo4j 容器持久化与增量摄入
- **步骤**：① 现有容器加 volume（`-v neo4j_data:/data`）重建，防数据丢失；
  ② `Neo4jStore` 增加增量 `upsert_shg`（按 extractor_version 的 Evidence 溯源
  去重）；③ 106 文档语料全量入库并跑级联统计查询。
- **依赖**：Docker（已就绪，容器 neo4j-metaphor 运行中）。**成本**：半天。
- **验收**：语料 1,050 chunk 入库，重跑幂等。

### D2 本体与资源发布打包
- **步骤**：① `ontology_default.json` + `ontology_bilingual.json` + 触发词表
  打成 HuggingFace dataset / GitHub release（含 schema 说明与来源声明）；
  ② 引用格式与许可（公版语料 + 自建标注）。
- **依赖**：A4（双语翻译人工抽检 D5）。**成本**：半天。
- **验收**：他人可一键加载（附加载脚本）。

### D3 MetaphorRAG-Bench 打包
- **步骤**：① 诊疗集（2 文档 + 金标）+ llmgold 改写查询基准（632 查询/金标/
  常识关联替代口径）+ 连贯文档语料（106 文档）统一打包；② 附评测脚本与
  排行榜格式。
- **依赖**：A4（人工抽查）先行。**成本**：1 天。
- **验收**：`pip install` 或 clone 后一条命令复现全部基准数字。

### D5 双语词典人工抽检
- **步骤**：AI 翻译的 835 域词抽 10%（84 词）人工核对；错误词修订回
  `bilingual_llm_ext.json`。
- **依赖**：1 名有英语背景的标注者。**成本**：1 小时。**验收**：抽检准确率
  ≥90%，错误修订入发布版。

---

## E. 更大工作项（P3，立项后再排）

### E1 英文通道 + VUA 跨语言
- **步骤**：① 英文触发词/语义域词表（对应 semfield.py）；② MetaNet 英文本体
  直连（不再经中文对齐）；③ VUA 数据下载（EducationalTestingService/metaphor）
  与 load_vua_csv 对接；④ 跨语言对比实验。
- **依赖**：无硬依赖，工程量 1–2 周。**验收**：英文语料上 P1 门槛复现。

### E2 RAG 端到端系统化
- **步骤**：把 demo_rag 扩成可交互的小型系统（106 文档语料 + 前端），
  生成侧引入框架一致性约束的 decoding 检查。
- **依赖**：B1/B4。**验收**：演示视频 + 用户试用反馈。

---

## 阻塞项追踪（外部条件）

| 阻塞 | 影响 | 解除动作 |
|---|---|---|
| glm-4.7 免费档 429 墙 | B2 跨模型补判 / C2 | 配额重置后增量补判（缓存支持），或换 DeepSeek/OpenAI key |
| DeepSeek key 401（已失效） | deepseek judge 交叉 | 新 key 或弃用（glm 交叉已覆盖） |
| api.conceptnet.io 502 | C3 原生复测 | 服务恢复后换装重跑（脚本就绪） |

## 建议节奏

- **第 1 周**：A2 + A3 + A4（人工抽查组织）+ B1（管线接入）→ 论文定稿候选。
- **第 2 周**：A1 译英 + C1 真人评估执行 + D2/D3 打包 → 投稿。
- **第 3–4 周**：B2/B3 补强统计 + C3 复测 + D1 增量管道。
- **之后**：E1 跨语言、E2 系统化。
