# 效果对比（初稿）

> **定位**：论文实验/对比章节的初稿底稿。汇总本框架全部已测效果，
> 按「抽取 → 构建 → 检索 → 消融 → 生成」流水线组织，并如实标注每个数字的
> 测量口径、样本量与可信度边界。
> **复现**：每个表都给出复现命令；除 §6 迭代实验外全部零 API 费用（LLM 缓存重放）。
> **日期**：2026-08-30（代码与数字同源，改动代码须重测）。

---

## 1. L1 抽取效果对比（CCL2018 测试集 n=1100，硬门槛 P1<15%）

| 配置 | 召回 | P1 字面误判率 | 精确率 | F1 | 达标 |
|---|---|---|---|---|---|
| 纯触发词（默认本体 7 级联/14 框架） | 3.1% | 1.3% | — | — | ✅ |
| + MetaNet 迁移 + 自举模式串 | 7.6% | 7.9% | 0.929 | 0.141 | ✅ |
| + 语义域失谐通道（规则侧天花板） | 9.7% | 7.9% | — | — | ✅ |
| **+ LLM 开放发现（阈值 0.85 + refine 校验）** | **69.6%** | **9.2%** | **0.990** | **0.818** | ✅ |
| LLM 开放发现（阈值 0.5，不设防） | 89.6% | 32.9% | 0.977 | 0.929 | ❌ |

复现：`python -m metaphor_graph.evaluate_real --use-metanet --use-bootstrap --use-semfield --use-llm --llm-batch 20 --llm-conf 0.85`（缓存重放零费用）。

**对比结论**：
- 规则侧在 P1<15% 约束下的召回天花板 ≈9.7%；裸名词触发可到 ~50% 但 P1 爆到 60%+。
- LLM 开放发现把召回拉到 69.6% 而 P1 不升——**帕累托前沿整体外推**。
- 双防线缺一不可：仅阈值 0.85 → P1 14.5%；加 refine 校验 → 9.2%。

## 2. 知识库构建效果（P0/P2 里程碑）

| 指标 | 自举前 | 自举后 | 清洗后（生产默认） | 门槛 |
|---|---|---|---|---|
| 本体框架数（P0） | 31 | 2185 | **2177**（core 299/longtail 1878） | ≥300 ✅ |
| L2/L3 层级覆盖率（P2） | 2.4% | 100% | **100%** | >85% ✅ |
| L2/L3 构建 LLM 调用（H2，每 1000 边） | ~187（聚类回退） | **~20（查表）** | 同左 | 降一个数量级 ✅ |
| 清洗影响（缓存重放三变体验证） | — | — | **逐指标持平**（P1/召回/覆盖不变）；core-only 召回 −1.2pp | — |

复现：`python -m metaphor_graph.ontology_clean --verify`。

## 3. 检索效果对比（核心章节）

### 3.1 三条检索通路 × 两种查询分布

金标：LLM 非构造金标（`evaluate_llmgold`，deepseek-v4-flash 判定，构造金标一致性 96.8%）。

| 检索通路 | 触发词重叠型查询（by-construction） | **改写型查询（触发词被程序化切断，n=632）** |
|---|---|---|
| 字面包含通路（B1 字面基线代理） | 0.000（n=8 诊疗集） | **0.000** |
| 触发词级联通路（U-Retrieval 粗筛） | 0.500（诊疗集） | **0.042** |
| **语义超图通路（MetaphorSHG 排序）** | 1.000 | **1.000** |

**结论（H3 量化版）**：改写查询下字面与触发词级联通路双双失效，语义超图检索是
唯一稳健的隐喻通路——「图结构提供了向量检索无法表达的关系」+「语义渲染的超边
排序承载了稳健召回」两条主张同时成立。

### 3.2 排序器对比（同一语义超图通路下的打分函数）

| 排序配置 | 构造金标（饱和区，n=861） | **LLM 金标·改写查询（有区分度，n=632）** |
|---|---|---|
| 人工加权（0.35/0.25/0.20/0.20） | MRR 0.995 | MRR 0.502 |
| 自监督训练（7 维） | MRR 0.998 | MRR **0.547** |
| LLM 弱监督训练（refine 否决负样本） | MRR 0.998 | MRR 0.536 / Hits@3 **0.641** |

**结论（H5 条件化）**：训练排序器的增益**取决于查询分布**——触发词重叠型查询
clue 特征恒命中（基准饱和，测不出差异，诊疗集上甚至「训练更差」）；改写型查询
下训练反超人工加权 **+4.8pp MRR**。自监督天花板（clue AUC≈1.0）经弱监督迭代
修复（1.0→0.931，leakage 标记 136→23）。

### 3.3 与方案 §6.2 基线系统的映射（诚实声明）

| 方案基线 | 本仓库的实现状态 | 代理证据 |
|---|---|---|
| B1 BM25+向量混合 | **部分实测**（字面通路 + 哈希向量相似度） | 改写查询 Recall 0.000（§3.1） |
| B2 GraphRAG（二元三元组） | ⛔ 未直接实现外部系统 | 结构代理：H3（去隐喻通路归零） |
| B3 HyperRAG（通用超图） | ⛔ 未直接实现 | 结构代理：H4（flat vs 跨层等价） |
| B4 MetaphorBoost 式一阶隐喻图 | ⛔ 未直接实现 | 功能代理：无层级版的 L2/L3 覆盖率与级联检索缺失 |
| B5 MetaphorMG（方案 B 元图） | ✅ 已实现（metagraph.py + U-Retrieval） | 机制演示（demo.py 验证环节） |
| B6 MetaphorSHG（方案 A） | ✅ 本文档全部数字 | — |

> 投稿前建议：B2/B3 若无法复现外部系统，论文应把对比主张收窄为
> 「组件级消融对照」（本文档 §4），不得暗示与外部系统的端到端对比。

## 4. 结构消融对比一览（P5）

| 消融 | 假设 | 预期 | 实测 | 结论 |
|---|---|---|---|---|
| A1 去类型安全约束 | H1 | P1 回升 >30% | P1 0.092 不变（拦 0+24 个错误绑定） | ❌ 约束管结构正确性，不管精度 |
| A2 L2/L3 改回 LLM 聚类 | H2 | 成本升 10× | 0→187 次/千边 | ✅ |
| A3 三通道消融 | — | — | 无 refine P1 43.4% → 有 refine 0.0% | refine 是精度防线 |
| A4 去自举本体 | — | 召回降 | −1.8pp | 价值在 P0 资源 |
| A5 去跨 chunk 扩展边 | — | 消歧下降 | P3 F1 1.000→0.000 | ✅ L1.5 是必要条件 |
| H4 检测信号来源分解（改口径） | — | — | 完整跨层 0.950 = 仅 L1 0.950 > 原始嵌入 0.150；GRU 1.000 | 信号来自 n 元性，层级零贡献 |
| A7 排序器训练 vs 人工 | H5 | 训练更优 | **条件化**：重叠型查询无差异；改写型 +4.8pp（§3.2） | ⚠️ 部分翻案 |
| A8 关自适应阈值 | — | — | 稀疏/稠密图均无差异（0.500/0.500；0.250/0.250） | 中性 |
| A9 去角色特征 | H7 | MRR 降 | 完全不变（0.998 vs 0.998；0.545 vs 0.545） | ❌ |
| A6 换通用常识知识 | 性能下降 | **常识关联通路 Recall@10=0.095 vs 隐喻通路 0.783**（n=652 改写查询；LLM 生成常识关联替代 ConceptNet 口径，987 概念词邻居已缓存） | ✅ A6 成立（8 倍差距） |

## 5. 端到端生成对比（`demo_rag.py`，deepseek-v4-flash 生成）

标杆问题「为什么我们的项目推进不动、寸步难行？」（答案 chunk 写的是「陷在泥潭」）：

| 上下文构造 | 检索命中 | 生成表现 |
|---|---|---|
| 隐喻结构上下文（超边 50%+实体 30%+chunk 20%） | ✅ c0 | 正确点明隐喻框架：「项目陷入『泥潭』（地形→项目困境）…」 |
| 纯向量对照（哈希向量 top3，无结构） | ✅ c0（共享字面 n-gram「我们的项目」） | 能作答但未提级联框架 |

> 诚实注记：对照组相似度随向量器升级——哈希 0.283、真实句向量 0.725（embedding-3
> 从语义上把「推进不动」与困境场景相连，也命中）。这修正了我们的预期：真实向量
> 让纯向量基线同样能跨域。隐喻结构的增益体现在 §3.1 的「字面与级联都断链」分布上
> （字面通路 0.000、级联 0.042、语义超图 1.000），而非与向量基线的单点对比。

## 6. 模型迭代记录（本轮，全部在 LLM 金标·改写查询基准上测量）

迭代动机：§3.2 的 LLM 金标基准是项目**第一个有区分度**的检索基准（此前全部
饱和于 0.99+），首次让「向量器 / 通路 / 排序器」的迭代效果可被度量。

**协议**：全量（n=632）做变体筛选；调参在偶数号文档（tune，n≈318）完成，
最终对比在**奇数号 held-out 报告集（n=314）**上读数——防测试集调参。

| 迭代项 | 变更 | 全量 MRR@10（筛选） | held-out MRR@10（最终） |
|---|---|---|---|
| R0 基线 | 哈希词袋 + 无回退 + npp=3 | 人工 0.502 / 自监督 0.547 | 人工 0.484 / 自监督 0.535 |
| R1 向量器 | 1–3 gram + 符号哈希（`NgramEmbedder`） | 人工 0.520 / 自监督 0.558 | ↓ 并入 R8 |
| R2 通路 | 触发词级联 + 语义回退 | 通路 Recall 0.042→**0.057** | ↓ 并入 R8 |
| R3 R1+R2 | 叠加 | 通路 Recall 0.061 | ↓ 并入 R8 |
| R4 排序器 | 自监督 ∪ 弱监督联合 | 联合 0.539 / Hits@3 0.649 | ↓ 并入 R8 |
| R5/R6 调参 | npp∈{5,1}（tune 切分） | npp=5 优（弱监督 0.561 vs 0.548） | ↓ 并入 R8 |
| **R8 最优配置** | ngram + fallback + npp=5 + 联合 | — | **人工 0.523 / 自监督 0.579** |
| **R9 真实句向量** | 智谱 embedding-3 @512 维（`--embedder real`） | 人工 0.707 / 自监督 **0.758** | **+21pp**（相对哈希全配置） |

**上下文打包消融（judge 敏感性发现）**：4 变体盲评中当前 50/30/20 打包以 26.50
胜出（纯向量对照 21.12），但此前 deepseek judge 下对照 6:1 胜——**LLM-as-judge
结论对 judge 模型高度敏感**，生成质量主张必须真人评估仲裁（组件级检索结论不受影响）。

**真实句向量是单项收益最大的迭代**（n=632 全量口径：人工 0.502→0.707、自监督
0.547→0.758，Hits@3 0.604→0.815）；训练增益在其上依然成立（+5.1pp）。从最初基线
（人工加权+哈希 0.502）到最终配置（自监督+真实句向量 0.758）**总提升 +25.6pp**。
P4-A 亦翻案：跨层传播从 −0.104（哈希伪影）变为 **+0.062**（真实向量下 §4.5 承诺成立，
见 README §7.4）。完整实测日志：`data/p4_real_embed.log`、`data/it_r9_real_embed.log`。

**迭代结论（held-out，n=314）**：
1. **总迭代增益 +9.5pp MRR**（人工加权基线 0.484 → 最优配置自监督 0.579）；
2. 三项迭代分解：向量器（+3.9pp on 人工加权：0.484→0.523）、训练排序器（同配置
   +5.6pp：0.523→0.579）、语义回退（级联通路 Recall 翻倍 0.020→0.042，绝对值仍低
   ——哈希级级联粗筛太糙，语义超图通路仍是正解）；
3. A9 再次复现：R8 上去角色（0.583）≥ 完整 7 维（0.579）；
4. 联合训练（0.558）未超过纯自监督（0.579）——弱监督的价值仍在训练集健康度；
5. **转正建议**：NgramEmbedder 建议转正为默认向量器（需按 §1–§5 重测全部已上报
   数字后统一替换；本初稿保持旧口径数字不变，避免同文档数字口径混杂）。

复现：`data/it_r*.log`（各 log 首部含完整 CLI 参数）；调参协议 `--split tune/report`。

## 6b. 连贯文档语料 L1.5 扩展隐喻验证（进行中）

**动机**：§4/§6 已证 L1.5 机制必要（A5）但在独立短句语料上天然稀疏（CCL2018
5 条/110 伪文档）。真实连贯文档（真实 RAG 场景）是 L1.5 价值域的最终检验。

**语料**（本轮自主采集，均为公开渠道，`data/corpus/`）：
- 财经新闻 40 篇（人民网经济频道，方案 §5.1 主语料域）
- 政府工作报告 6 篇（维基文库，中央+省级，隐喻密集官方话语）
- 鲁迅全集散文 60 篇（公版文学，新颖隐喻域）
- 共 106 篇文档 / 1,050 chunk（500 字 + 10% 重叠，方案 §4.6 口径）

**管线**：LLM 开放发现（GLM-5.3-flash，reasoning=low，批量 20 句/请求，真实调用
落盘缓存）→ refine 校验 → 清洗本体绑定 → 逐文档建图。

| 指标 | CCL2018（独立短句） | 连贯文档语料（n=106 文档/822 L1） |
|---|---|---|
| 含扩展链文档占比 | 4.5%（5/110） | **30%（32/106）** |
| 扩展链 / 百条 L1 | 0.6% | **6.3%（52 条）** |
| 分语料 | — | 财经 8.2%（29 条/354 L1）、政报 4.3%（7/163）、鲁迅 5.2%（16/305） |

**结论**：连贯文档上扩展链密度是独立短句语料的 **10 倍**，30% 的文档含跨 chunk
扩展链——L1.5 的价值域假设（连贯文档 vs 句级语料）得到定量证实。抽样链内容合理
（战争级联跨 chunk、培育/根基/深耕农业框架跨段、金融域跨段）。诚实注记：鲁迅
语料（1920 年代白话）L1 抽取率低（现代触发词表覆盖不足），其密度 5.2% 是在
低 L1 基数下取得——跨语时代泛化是已知边界。管线与测量：`evaluate_document_corpus.py`
（LLM 真实调用 GLM-5.3-flash ~¥1.6，全部落盘缓存可重放）。

**链质量评审（`evaluate_chain_quality.py`，judge=glm-5.3-flash，n=30 抽样）**：
严格 MIPVU 口径下链精度 **16.7%**（5/30；文学 38% / 政报 17% / 财经 6%）。
失败模式清晰：① 词化/惯用语（"基础上""聚焦""扎根"）被计为隐喻提及；② 单点
隐喻在相邻 chunk 重复出现即被合并，无「跨段延续」检验。**改进尝试与结论（三轮）**：
1. 文本级判据 `vehicle_repeat`（触发词跨 chunk 词面复现或喻底交集≥2）已实现：
   诊疗集 P3 F1 双判据均 1.000,但语料密度仅 52→49——存活链不变,**文本级信号
   区分不了词化惯用语与单点共现,无效**。
1b. **图连贯度过滤无效**（`--coherence-threshold 0.5`）：在 L1-only 图上计算
   候选链 (源域,目标域) 的 metaphor_coherence——全体中位数 0.820,阈值 0.5
   保留 52/52,精度不变 16.7%。原因：候选链的域对本就通过自身框架相连,
   抽象域标签的图连贯度对「词化惯用语 vs 真隐喻」不敏感。
2. **语义级校验已接入并闭环**（`llm_backend.verify_chains_batch` +
   `--llm-verify`）：LLM 逐链判定「跨段延续」（MIPVU 严格口径写入提示词）,
   52 条候选**通过 11 条（21.1%）**;保留集全量评审精度 **63.6%**（7/11;
   跨模型配对子集 71.4% = 10/14）——相对未过滤基线 16.7% 提升约 4 倍。
   诚实注记：judge 混合（8 条 glm-4.7 跨模型 + 3 条 glm-5.3 同模型）;
   校验与抽取同模型（glm-5.3）;「密度换精度」明确（52→11,但保留的
   恰是值得进入 RAG 上下文的链）。金标稳健性双协议：跨模型 κ=0.965（114 边对）+
   同模型换序 κ=0.912（420 边对）。

## 7. 里程碑记分卡（对齐方案 §八）

| 阶段 | 验收标准 | 实测 | 状态 |
|---|---|---|---|
| P0 资源建设 | 种子 ≥300 框架 | 2177（清洗后，core 299） | ✅ 超额 |
| P1 L1 抽取 | 字面误判率 <15% | **9.2%** | ✅ Go/No-Go 通过 |
| P2 超超图构建 | L2/L3 覆盖率 >85% | **100%** | ✅ |
| P3 扩展隐喻 | 跨 chunk 消歧 >70% | F1 1.000（诊疗集；全量短句语料扩展链天然稀疏） | ✅ 机制验证 |
| P4 HGNN 检测器 | 抽取 F1 +5pp | 口径重定：图结构校验信号 0.95（H4 分解：来自 n 元性） | 🟡 已诚实收口 |
| P5 消融 | 6 组符合预期 | **全部 6 组完成**：H1–H4 + A5–A9；H1/H5(条件化)/H7 证伪，H2/H3/A5/A6 成立 | ✅ |
| P6 论文 | 投稿稿件 | **初稿 v0.1**（`论文初稿.md`）；打包消融揭示 **judge 敏感性**（deepseek 判对照胜 / glm 判隐喻结构胜）→ 生成质量主张须真人仲裁 | ⏳ 迭代中 |

> 向量器口径说明：§1–§5 数字基于哈希向量（旧默认）；§6 R9 起为真实句向量
> （embedding-3）。NgramEmbedder/真实向量转正为默认前，须统一重测 §1–§5。
