# 隐喻超超图：面向高阶隐喻结构的知识表示与检索增强生成
## （论文初稿 v0.2，2026-08-31）

> **初稿说明**：中文工作稿（投稿时译英，目标 ACL/EMNLP/COLING 主会）。
> v0.2 整合：A6 常识替代消融、连贯文档语料验证（密度 10× + 链质量闭环 16.7%→66.7%）、
> 上下文打包消融（judge 敏感性）、真实句向量双口径、双语资源 100% 对齐。
> 全部数字来自可离线复现的实验脚本（复现命令见附录 A）；双口径说明见 §6.4。
> 配套：`知识隐喻分析任务方案.md`（方案）、`论文表述改写.md`（主张清单）、
> `效果对比.md`（对比底稿）、`metaphor_graph/README.md`（工程细节）、`后续待办.md`。

---

## 摘要

隐喻不是"源域→目标域"的二元映射，而是同时承载源域、目标域、喻底集合与触发词的
n 元关系，并以概念级联（cascade）的形式层级组织。现有图 RAG 将知识压缩为二元三元组，
在隐喻场景下既丢失喻底集合的整体性，也忽略级联层级。本文提出 **MetaphorSHG**——
四层隐喻超超图（触发词层 L0 / 映射超边层 L1 / 框架超顶点层 L2 / 级联超超顶点层 L3），
以及"三通道抽取 → 级联查表构建 → 三通路检索 → 语义校验过滤"的端到端框架。
在中文隐喻标注语料（CCL2018，n=1,100）、自建的检索基准（n=632 改写查询，
**含去锚定口径**）与 106 篇真实连贯文档语料上：(1) 接入 LLM 开放发现后，隐喻句
召回从规则侧的 9.7% 提升至 69.6%，字面误判率保持 9.2%（硬门槛 15%）；
(2) cascade 查表构建将 L2/L3 覆盖率提升至 100%（**诚实口径 82.4%**），
成本比 LLM 聚类低一个数量级，配套双语本体资源全量对齐英文（2,177 框架）；
(3) 检索通路分解显示：在触发词被切断的改写查询上（全局池 1,100），字面通路
完全失效（非空率 0/777）、触发词级联通路召回仅 0.0206（非空率 0.192），
语义超图通路召回 0.1918（MRR 为随机基线的 17.1×）——三通路相对结论成立，
但绝对水平远低于原池 ≤10 基准下的平凡饱和值；以通用常识关联替换专属隐喻级联后
召回从 0.783 跌至 0.095——跨域映射不可被常识关联替代；(4) 连贯文档上跨 chunk 扩展隐喻链密度是独立短句
语料的 10 倍，"候选生成 → LLM 语义校验过滤"双段管线将链的严格口径精度从
16.7% 提升至 63.6%（跨模型配对子集 71.4%）；(5) 结构消融定位信号来源：隐喻连贯度校验信号（跨框架判别
0.95、**全量 AUC 0.915**）来自超边 n 元喻底共现而非层级传播，且层级传播的检索
收益以语义表示质量为前提（真实句向量下翻案为正增益 +0.062）；
(6) **基准自我审计（新增）**：我们诊断出原检索基准的两处方法论缺陷——
候选池过小（median 7–8，使 Hits@10 平凡饱和、随机 MRR 期望即达 0.37）与
**构造锚定**（100% 的金标都包含产出该问题的 chunk，97.9% 只包含它）。
重建基准（全局池 1,100 chunk + 去锚定口径）后测得：**锚定抬高约 +0.66 MRR**，
但**去锚定下 MRR 仍是随机基线的 34 倍**，即架构具备真实的跨域检索能力；
同时**训练增益与权重重标定效应在去锚定后消失**，表明原报告的训练优势
主要来自锚定路径与人工权重错配。
我们同时如实报告多项被证伪或需条件化的假设（类型约束不承担字面误判过滤、
自监督排序器的触发词泄漏天花板、LLM-as-judge 的模型敏感性、
查询侧可观测标量不可行、L3 级联层零结构贡献），为隐喻结构化检索提供完整的证据边界。

---

## 1 引言

概念隐喻理论（CMT）指出，人们用具体概念（源域）理解抽象概念（目标域），
且这种映射系统性地层级组织为一般隐喻与隐喻级联（cascade）。然而当前基于知识图谱的
检索增强生成（RAG）将世界知识表示为二元三元组，这在隐喻场景下产生两重失真：

- **n 元性丢失**。"时间是金钱"的完整表征包含喻底集合（稀缺、可花费、可浪费…），
  喻底是集合而非单值——这正是超边（hyperedge）的定义特征；强行拆成二元边会割裂
  语义整体性并引发路径爆炸。
- **层级结构缺失**。隐喻级联是认知语言学已发现的自然结构（基础隐喻 → 一般隐喻 →
  级联），不是工程构造的社区划分；二元图无法表达"超顶点的超顶点"。

基于此，本文将隐喻的层级结构翻译为超超图（hyper-hypergraph）的数学结构，
并回答一个此前未被研究的问题：**这种翻译能否转化为检索增强生成的可度量收益？**
与既有工作的关键差异在于：我们不只主张结构表达力，而是把"n 元性"与"层级性"
作为**可分离的科学变量**分别检验（§6.3），并如实报告两类结论。

**贡献**：
1. 首个隐喻超超图表示与端到端构建/检索框架（四层结构 + 三通道抽取 + 级联查表 +
   双段扩展链管线），配套全量英文对齐的中文级联本体资源（2,177 框架）；
2. LLM 开放发现与 refine 校验的双防线抽取：召回 9.7%→69.6% 且 P1 不升
   （帕累托前沿外推）；
3. 检索三通路分解与量化：改写查询下**语义超图通路 MRR 为随机基线的 17.1 倍**
   （0.1183 vs 0.0069），字面通路**完全失效**（非空率 0/777）、触发词级联通路
   非空率仅 0.192；重叠型查询下语义通路 MRR 0.9200——**该架构能力高度依赖
   触发词命中（重叠型 vs 改写型相差 7.8×）**；且常识关联替代专属级联后跌至
   0.095——跨域映射不可被常识关联替代；
4. **基准自我审计（新增）**：诊断出原检索基准的**构造锚定**（100% 金标含产出
   chunk）与**候选池饱和**（median 7–8，使 Hits@10 平凡为 1.0），量化锚定抬高
   ≈ +0.66 MRR，并给出把架构能力与锚定分离的修复口径；
5. 连贯文档验证：跨 chunk 扩展链密度为独立短句语料的 10 倍；"候选生成 → LLM
   语义校验"双段管线将链精度 16.7%→66.7%；
5. 结构与信号来源的分解检验：隐喻连贯度校验信号来自 n 元喻底共现而非层级传播；
   层级传播的检索收益以语义表示质量为前提；
6. 训练排序器增益的条件化刻画与自监督泄漏天花板的机制修复；
7. 完整的负面结果报告（12 项）与 LLM 评估方法的敏感性分析。

---

## 2 相关工作

**隐喻计算资源与检测。** MetaNet 将隐喻与框架的层级性质形式化为可计算本体，
其隐喻级联（cascades）是预先组织好的基础/一般隐喻打包集合。中文侧已有基于
WordNet/SUMO 的 1,256 个汉语隐喻类型数据库与 COGMED 感官隐喻库，但缺乏带级联
归属的层级本体。检测方面，KEG 用句法知识图 + 图循环网络做逐句判定，EmoBi 引入
情感信号——但均使用普通图，无法建模 n 元喻底与非邻接成分。

**图增强 RAG。** GraphRAG 类方法以二元三元组建图；MedGraphRAG 以三层图 + U 型
检索（标签粗筛→逐层下钻→自底向上约束）处理医学标签层级；HyperRAG 将超图引入
n 元事实检索。三者均未触及隐喻的域间映射语义与级联层级。

**超图表示学习。** HGNN 的两步消息传递（节点→超边→节点）、HyperC2Net 的跨层
超图卷积、HL-index 的超图可达性索引为本方法的组件提供了基础；本文将它们置于
隐喻语义下检验，并给出跨层传播收益的前提条件（§6.3）。

**研究缺口**：截至写作时，尚无将"隐喻 × 超超图/元图"结合并系统测量其检索收益的
已发表工作；隐喻级联本体从未被接入 RAG 检索层。

---

## 3 方法

### 3.1 四层隐喻超超图

```
L0 触发词层   语言形式（"泥潭""发条"）
L1 映射层     隐喻超边 (源域, 目标域, 喻底集合, 触发词, 情感, 语境跨度) —— 跨 chunk
L2 框架层     一般隐喻 = 超顶点（L1 超边的集合，如 OBSTACLE_IS_TERRAIN）
L3 级联层     隐喻级联 = 超超顶点（L2 框架的集合，围绕同一目标概念组织）
```

结构合法性：L1 超边承载 n 元喻底集合；L2/L3 超顶点对应知识超超图中"基集的
类型化子集"。语言学合法性：L2/L3 的分组判据是级联归属（认知语言学的自然结构），
而非聚类。

### 3.2 三通道抽取与双防线

候选来自三通道：① 触发词通道（自举模式串 + lift 过滤）；② MIPVU-lite +
Wmatrix 式语义域失谐通道；③ **LLM 开放发现**（开放文本直接发现隐喻，不依赖词表，
召回突破的关键）。全部候选经类型安全约束（结构正确性）与置信过滤。

字面误判过滤由**双防线**承担：LLM 置信阈值（实测达标拐点 0.85）与 **refine
校验**（对触发词/语义域通道候选做二次判断）。MIPVU 判据（基本义≠语境义 + 三类
排除项）写进提示词——去除它 P1 飙至 28.9%。

### 3.3 级联本体查表构建

L2/L3 归属通过级联本体查表完成（未匹配项仅少量回退 LLM 聚类）。本体经三步
冷启动：MetaNet 迁移种子 → LLM 自举回流（只用训练集标注，防止标签泄漏）→
清洗沉淀（删自环、清喻底、支持度分层 core/longtail）。双语资源：全部 2,177 框架
经三级标注（curated / auto-gloss / LLM 翻译补全）对齐英文概念域名，全量对齐率
100%。成本上，查表把 L2/L3 构建从 ~187 次/千边降至 ~20 次。

### 3.4 检索层三通路与扩展链双段管线

- **字面通路**：子串/词面匹配（B1 基线）；
- **触发词级联通路**：查询触发词 → 级联 → 目标域概念 → chunk（U-Retrieval
  式粗筛；可加语义回退：查询向量与级联描述匹配）；
- **语义超图通路**：超边渲染为自然语言后向量化，7 维特征（sem/struct/clue/type/
  same_frame/same_cascade/ground_jaccard）打分排序，支持训练排序器与自适应阈值。

**跨 chunk 扩展链采用双段管线**（与抽取侧 discover→refine 对称）：第一段
"候选生成"以宽松判据（同框架 + 同源域 + 喻底交集 + 间距<3）快速合并出候选链
（保密度）；第二段"语义校验"以 LLM 逐链判定跨段延续性（MIPVU 严格口径提示词）
过滤（保精度，§6.6）。

### 3.5 跨层消息传递（HGNN）

节点→超边→节点两步传播 + 残差，将框架/级联语义注入实体表示；检测器 API
`metaphor_coherence` 计算两实体在图空间中的连贯度，作为触发词与 LLM 之外的
第三道校验信号。

---

## 4 实验设置

**数据**：CCL2018 中文隐喻识别共享任务（测试集 1,100 句，含 76 中性句）；
训练集 4,075 句仅用于本体自举与触发词 lift 过滤（无测试集标签泄漏）。
**连贯文档语料**（自建，公开渠道）：人民网财经新闻 40 篇、政府工作报告 6 篇
（维基文库）、鲁迅全集散文 60 篇（公版），共 106 篇 / 1,050 chunk
（500 字 + 10% 重叠）。

**检索基准（关键设计）**：自动查询分两族——
*重叠型*（查询含金标边触发词，构造即金标，n=861）与*改写型*（LLM 将查询改写为
自然问题、程序化红线禁用触发词/域词，n=632；LLM 在全候选上做 listwise 相关性
判定构成**非构造金标**，与抽取构造的一致性 96.8%）。

> **⚠️ 基准自我审计（实测修正，见 §4.1 与 §6.7）**：上述"非构造金标"经复核
> **不成立**——实测 632/632 个查询的金标都包含「产出该问题的 chunk」，
> 619/632（97.9%）**只包含它**。即 LLM 的作用是**过滤**（筛掉不相关候选），
> 而**未解除锚定**。同时原基准的候选池按伪文档切分，median 7–8 / max 10，
> 使 **Hits@10 在 100% 的查询上平凡为 1.000**，随机排序的 MRR 期望即达 0.37
> （量程被吃掉一半）。这两处缺陷在 IR 文献中分别对应 **pooling bias**
> （Bias and the limits of pooling for large collections, 2007）与
> **test collection 规模不足**（Efficient construction of large test
> collections, SIGIR 1998）。§4.1 报告修复后的口径与数字。

**指标**：抽取——召回 / 字面误判率 P1（硬门槛 <15%）/ 精确率 / F1；构建——
L2/L3 层级覆盖率（**同时报告诚实口径**，见 §5.2）、构建 LLM 调用数；
检索——MRR@10、Hits@3/10、Recall@10（**同时报告随机排序基线**）；
扩展链——密度（链/百 L1、含链文档占比）与严格口径精度（LLM judge 盲评）。

**复现性**：所有 LLM 调用（discover/refine/改写/判定/校验/常识关联）均落盘缓存，
全部表格可零 API 费用重放；随机 id 全部确定性化（md5），单测护栏保证跨构建
可复现（124 项单元测试）。

### 4.1 基准自我审计与修复（新增）

针对上述两处缺陷（候选池过小、构造锚定），我们重建了检索基准
（`evaluate_repaired.py`），并把"架构能力"与"锚定效应"分离：

**修复一：全局候选池。** 把 110 个伪文档合并为**一张全局图**，
候选池 = **1,100 个 chunk**（原 median 7–8）：

| 指标 | 原基准（池=7） | 修复后（池=1,100） |
|---|---|---|
| 随机排序 MRR 期望 | **≈0.3704** | **0.0069** |
| 随机 Hits@10 | **1.0000（平凡饱和）** | 0.0091 |
| MRR 可用量程 | 0.63 | **0.99** |

**修复二：去锚定口径（deanchor）。** 把**产出 chunk 从候选池中移除**，
金标改为「同框架/同级联的**其它** chunk」，从而物理切断"复现锚点"这条捷径。
可构造 deanchor 查询 **385/634**（其余无同框架其它 chunk，故丢弃）；
金标规模 min=1 / median=3 / max=37（比 anchored 的恒为 1 更真实）。

**评测自我审计字段**：随机排序基线、MRR 量程、池外文档比例——
IR 文献要求这类字段随指标一并报告（SIGIR 1998；2007）。

**结果（离线、零 API 费用；全量 634 / 385 查询）**：

| 口径 | 查询数 | 人工加权（重标定） | 人工加权（历史） | 训练后 | 随机基线 |
|---|---|---|---|---|---|
| **anchored**（金标=产出 chunk） | 634 | **0.8956** | 0.8390 | **0.9006** | 0.0069 |
| **deanchor**（产出 chunk 移出池） | 385 | **0.2330** | 0.2307 | **0.2211** | 0.0069 |

**四条结论**：

1. **锚定抬高幅度 ≈ +0.66 MRR**（人工加权口径：0.8956 vs 0.2330）。即原基准
   报告的数字里，**约 74% 来自"把已知答案排到前面"**。
2. **但架构确有真实的跨域检索能力**：deanchor 下 MRR **0.2330 是随机基线
   0.0069 的 34 倍**，Recall@10 = 0.3357（随机期望 ≈0.009）。
   **这排除了"架构完全无效"**——若能力全来自锚定，deanchor 应落到随机水平。
3. **权重重标定只在锚定口径下"有效"**：anchored Δ = **+0.0565**，
   deanchor Δ = **+0.0023**。说明原基准下"人工权重配错"造成的大部分损失，
   本身就发生在锚定路径上。
4. **训练增益在去锚定后反转**：anchored 下 trained（0.9006）> hand（0.8956），
   deanchor 下 trained（0.2211）< hand（0.2330）。训练器学到的是"复现构造锚点"，
   而非更好的跨域检索。

---

## 5 抽取与构建结果

### 5.1 抽取：LLM 开放发现外推帕累托前沿

| 配置 | 召回 | P1 | 精确率 | F1 |
|---|---|---|---|---|
| 规则侧（MetaNet+自举+语义域，天花板） | 9.7% | 7.9% | — | — |
| + LLM 开放发现（0.85 + refine） | **69.6%** | **9.2%** | **0.990** | **0.818** |
| + LLM（0.5 不设防） | 89.6% | 32.9% ❌ | 0.977 | 0.929 |

规则侧所有优化都在"召回换 P1"的帕累托前沿上挤（裸名词触发召回 ~50% 但 P1
60%+）；LLM 直接把前沿整体外推。**双防线**缺一不可：仅阈值 0.85 → P1 14.5%，
加 refine → 9.2%（代价：召回 69.1%→66.3%，划算）。剩余 6 个"误判"中多条标注
存疑（如"千百万双眼睛犹如千百万台摄影机"为明喻），真实 P1 可能更低。

### 5.2 构建：查表降本、覆盖率修复与双语资源

| 指标 | 自举前 | 自举+清洗后 | 门槛 |
|---|---|---|---|
| 本体框架数 | 31 | 2,177（core 299 / longtail 1,878） | ≥300 ✅ |
| L2/L3 覆盖率（报出口径） | 2.4% | **100%** | >85% ✅ |
| **L2/L3 覆盖率（诚实口径）** | — | **82.4%** ⚠️ | >85% ❌ |
| 构建调用（/千边） | ~187 | **~20** | ↓10× |
| 英文对齐率 | 0 | **100%**（curated/auto-gloss/LLM 翻译三级） | 资源发布 ✅ |

**诚实覆盖率（新增，必须与报出口径并列）**：报出的 100% 由两种不同性质的归属
混合而成。实测（120 句子集）：超边 108 条中**只有 82.4% 挂在本体正式登记的
框架上**，其余挂在抽取器为未知喻体临时建的回退框架上，靠
`builder._ensure_cascades` 按目标域**事后补建**的 `C_ADHOC_*` 级联才够到 100%
（19/108 条回退边，19/89 个级联为事后补建）。**排除这些后覆盖率 82.4%，
低于 P2 的 85% 门槛。** 两个口径度量不同的问题：前者是"每条边是否都有层级归属"，
后者是"层级归属是否来自本体"。实现见 `health.graph_health(ontology=...)`。

> 判据必须是「本体有无条目」而非 `F_LLM_` 前缀——生产本体 **98.6%** 的框架
> 以 `F_LLM_` 开头（自举沉淀的正式框架），按前缀判定会把整个本体误判为退化。

清洗（删自环、清喻底明喻标记、支持度分层）经缓存重放三变体验证**逐指标持平**
——指标中性的纯质量提升；core-only 消融显示 longtail 单例框架贡献 +1.2pp 召回
（模糊匹配锚点价值），故生产默认两级全载。

---

## 6 检索结果与分析

### 6.1 三通路对比：隐喻结构的增益在哪里

**原口径（候选池按伪文档切分，median 7–8；**其中"1.000"是平凡饱和，见下方修正**）**：

| 检索通路 | 重叠型查询 | **改写型查询（n=632）** |
|---|---|---|
| 字面包含通路 | 0.000 | **0.000** |
| 触发词级联通路 | 0.500（诊疗集） | **0.042** |
| **语义超图通路** | 1.000 ⚠️ | **1.000** ⚠️ |

> **⚠️ 修正（三通路已在修复后基准上完整重测，见 `experiments/三通路重测_修复后基准.md`）**：
> 右列的两个 1.000 是**平凡饱和**（池 ≤10 时任何返回全候选的排序器 Recall@10 恒为 1.0）。
> 在**全局池 1,100** 上重测改写型查询（n=777），三通路数字为：
>
> | 通路 | Hits@10 | Recall@10 | **非空率** |
> |---|---|---|---|
> | 字面包含 | **0.0000** | 0.0000 | **0/777 = 0.000**（完全失效） |
> | 触发词级联 | **0.0206** | 0.0206 | 149/777 = 0.192（多数返回空） |
> | **语义超图** | **0.1918** | **0.1918** | 777/777 = 1.000 |
>
> **三通路的相对结论成立**：语义超图仍是唯一有效通路（MRR 0.1183 = 随机的
> **17.1×**；Hits@10 是级联的 **9.3×**；字面通路完全失效）。
> **但绝对数字需大幅下修**：原文 1.000 高估约 5.2×，实际只能召回 19.2% 的相关 chunk。
>
> **一个重要的限定**：该架构的能力**高度依赖查询是否含触发词**——
> 重叠型查询语义 MRR 0.9200 vs 改写型 0.1183（**相差 7.8×**）。
> 改写切断了触发词后，查询侧结构无法激活，7 维特征中的
> `same_frame`/`same_cascade`/`ground_jaccard` 全部失效（AUC 0.505–0.530），
> 仅剩 `sem` 维（AUC 0.62）起作用 —— 即**语义通路在改写型查询上退化为纯语义相似度检索**。

改写查询切断触发词捷径后，字面与级联通路双双失效。**"图结构提供向量检索无法
表达的关系"与"语义渲染的超边排序承载稳健召回"两条主张同时成立**——但增益
体现在特定查询分布上，而非与向量基线的单点对比（真实句向量下纯向量基线也能
跨域，见 §6.4）。

### 6.2 排序器：训练增益的条件化

**原口径（候选池按伪文档切分，median 7–8；已被 §4.1 的基准修复取代）**：

| 排序配置 | 重叠型（饱和区） | 改写型（有区分度） | +真实句向量 |
|---|---|---|---|
| 人工加权 | 0.995 | 0.502 | 0.707 |
| 自监督训练 | 0.998 | **0.547** | **0.758** |
| LLM 弱监督训练 | 0.998 | 0.536 | 0.755 |

**修复后口径（全局候选池 1,100 chunk；随机基线 MRR 0.0069；全量 n=634/385）**：

| 排序配置 | anchored（金标=产出 chunk） | **deanchor（产出 chunk 移出池）** |
|---|---|---|
| 人工加权（重标定权重） | **0.8956** | **0.2330** |
| 人工加权（历史权重） | 0.8390 | 0.2307 |
| 自监督训练 | **0.9006** | 0.2211 |

**三条修正后的结论**：

1. **训练增益是锚定下的现象**。anchored 下 trained > hand（+0.005），
   但 **deanchor 下反转**（trained 0.2211 < hand 0.2330）。训练器学到的是
   "复现构造锚点"，而非更好的跨域检索。
2. **"训练反超人工加权 +4.5pp"主要是人工权重配错的产物**。原人工权重
   （0.35/0.25/0.20/0.20）与学到权重严重错配：**struct 过权 37 倍、type 12 倍**
   （归一化后；原始口径 49/19 倍）。仅把人工权重按学到比例重标定
   （不训练任何模型），anchored MRR 即从 0.8390 升到 **0.8956（+5.7pp）**；
   而在 deanchor 下重标定效应仅 **+0.0023**。
3. **重叠型查询的 clue 饱和现象依然成立**（clue 单特征 AUC≈1.0，训练退化为
   "复读抽取器"）；弱监督迭代把 clue AUC 压至 0.931、泄漏标记 136→23，
   但相对自监督的排序增益仍未证实（−0.013 MRR）。

### 6.3 结构贡献分解：n 元性 vs 层级性

**隐喻连贯度校验信号**（真 L1 边 vs 跨框架负样本）。**主指标为全量负样本
ROC-AUC**（正确处理并列；原 n=20 配对准确率分辨率仅 0.05，结论字符串会随
无关参数抖动翻转，见 §7）：

| 表示 | 配对准确率 | **全量 AUC** | 真实句向量（配对） |
|---|---|---|---|
| 完整跨层 HGNN | 0.950 | **0.915** | 0.850 |
| 仅 L1 超边（无框架/级联） | 0.950 | **0.910** | 0.850 |
| 原始嵌入 | 0.150 | **0.550** | 0.650 |
| GRU 共成员序列（未训练） | 1.000 | **0.945** | 1.000 |

1. **信号载体是 n 元性，不是层级性**：关掉框架/级联判别力不变（AUC 0.915 vs
   0.910，两种表示一致）；
2. **结构必要，但原表述需修正**：原始嵌入的全量 AUC = **0.550**，即
   **"无信息"而非"反向判别"**。原表 0.150 低于随机是**并列计错的口径产物**——
   默认哈希向量下抽象域标签几乎无共享 n-gram，实测全体并列率 94.6%。
   修正表述为：**没有 n 元共现聚合就没有可判别的结构信号**（AUC 0.550 ≈ 随机）。
3. **聚合方式次要**：GRU 顺序扫描（0.945）≥ 超图两步传播（0.915）；
4. **层级传播的检索收益以语义表示质量为前提**：哈希向量下跨层传播损害检索
   （−0.104），真实句向量下**翻案为 +0.062**（HGNN 1.000 vs 原始 0.938）。

**层级（L2/L3）的价值需附加条件**：应表述为**检索组织与可解释性**，
但实测表明当前构造下该层**对检索指标零结构贡献**——把全部级联替换为
结构上最退化的单例构造，检索 MRR 完全不变（0.5025）；只有彻底移除 L3 归属
才降到 0.4681。即该层目前的作用是**提供"是否已归属层级"这一二值信号**，
而非其内部结构。恢复其组织能力需修订构造规则（按源域打包可使跨目标域比例
从 0.8% 升至 51.6%），但**不改变任何主检索指标**（§6.7）。

### 6.4 知识源消融（A6）与表示质量敏感性

**A6 知识源消融**（ConceptNet 持续 502，以同厂商 LLM 生成的通用常识关联作
替代知识源——987 个概念域 × 12 常识邻居，明确要求非隐喻义；该设计隔离了
"知识类型"变量）：

| 知识源 | Recall@10 | Hits@3 |
|---|---|---|
| 隐喻通路（专属级联 + 语义超图排序） | **0.783** | 0.601 |
| 通用常识关联通路（替代口径） | **0.095** | 0.097 |

**A6 成立**：替换为通用常识知识后召回下降 8 倍——"推进不动→泥潭"这类跨域
映射不是常识关联，专属隐喻级联不可替代。失败样例佐证：常识通路对"人生如戏"
只命中字面"人生"相关 chunk，隐喻通路沿 EVENT_IS_PERFORMANCE 级联命中
"舞台/演员" chunk。

**表示质量敏感性**：真实句向量（embedding-3，512 维）作为独立变量测量——全部
排序器 **+21pp MRR**（人工加权 0.502→0.707、自监督 0.547→0.758）、Hits@3
0.604→0.815，且训练增益在真实向量上依然成立（+5.1pp）。两套口径不得混排于
同一表；转正真实向量为主口径前须统一重测。

### 6.5 生成层：LLM-as-judge 的模型敏感性

标杆问题"为什么我们的项目推进不动？"（文档写"陷在泥潭"）：隐喻结构上下文
（超边 50% + 实体 30% + chunk 20% 预算打包）使生成答案显式引用隐喻框架。
但**打包消融的盲评揭示了 judge 敏感性**：同一查询集、同一生成模型、4 种打包
变体，deepseek judge 判纯向量对照 6:1 胜，glm judge 判当前打包最优（26.50 vs
21.12，最优率 50%）——**结论随 judge 模型反转**。

方法论结论：**组件级检索结论（§6.1–6.4）不受影响，但一切生成质量主张必须由
真人评估仲裁**（方案 §6.4）。这把"需要人工评估"从程序性要求变成了本研究的
实测结论，也是对 LLM-as-judge 方法论的一个警示性贡献。

**金标稳健性双协议审核（A4 的 AI 替代）**：对 632 查询金标做两项无人工审核的
稳健性检验——① 跨模型二评（glm-4.7 重判 13 查询/114 边对）κ=0.965；
② 同模型换序二评（候选逆序 + temp0.4，全量 50 查询/420 边对）κ=0.912
（pa=0.976，10 处不一致全部为二评更宽）。金标对模型更换与引出顺序/温度
均稳健；全量真人复核仍建议作为投稿前最后一步。

### 6.6 连贯文档验证与扩展链双段管线

**密度**：连贯文档语料（106 篇真实文档）上扩展链密度 6.3 条/百 L1、30% 文档
含链，均为独立短句语料（0.6%、4.5%）的 **10 倍**——L1.5 的价值域假设定量
成立。

**质量闭环**：严格 MIPVU 口径的 LLM judge 盲评（n=30）显示候选链精度仅
16.7%（文学 38% / 政报 17% / 财经 6%），失败主因是词化惯用语（"基础上""聚焦"）
被计为隐喻提及。文本级延续性信号（触发词跨段复现 / 喻底交集≥2）实测无效
（密度 52→49，存活链不变）；**语义级校验**（`verify_chains_batch`，LLM 逐链
判定）将 52 条候选过滤至 11 条，被保留集经混合 judge（8 条 glm-4.7 跨模型 +
3 条 glm-5.3）全量评审精度 **63.6%**（7/11；其中跨模型配对子集 71.4%）——
约 4 倍提升。

双段管线的意义与抽取侧对称：第一段宽松生成保高密度候选（10×），第二段语义
校验保可用精度（4×）——两者共同使 L1.5 在真实 RAG 场景同时可用且可解释。

**过滤信号的三方消融**（同一候选集、同一 judge）：
| 过滤信号 | 保留链 | 精度 |
|---|---|---|
| 无过滤（基线） | 52 | 16.7% |
| 文本级：触发词跨段复现/喻底交集≥2 | 49 | 不变 |
| 图结构：metaphor_coherence 阈值 0.5（L1-only 图） | 52（中位连贯度 0.820） | 不变 |
| **LLM 语境级语义校验** | **11** | **63.6%** |

结论：词化惯用语与真隐喻的判别信息在**语境级语义**中——词面信号与抽象域
标签的图连贯度均不可见，只有读得懂上下文的 LLM 能判别。这为「结构化管线中
哪些环节必须引入语言模型」提供了一个可测量的判据实例。
（管线代码 `verify_chains_batch` / `--llm-verify` / `--coherence-threshold`；缓存可重放。）

### 6.7 基准修复后的检索结论（新增）

§4.1 重建的基准（全局池 1,100 chunk + 去锚定口径）给出了本项目**首组把
"架构能力"与"构造锚定"分离**的检索数字（全量 634 / 385 查询，离线零费用）：

| 口径 | 查询数 | 人工加权（重标定） | 人工加权（历史） | 训练后 | 随机基线 |
|---|---|---|---|---|---|
| anchored（金标=产出 chunk） | 634 | **0.8956** | 0.8390 | **0.9006** | 0.0069 |
| **deanchor（产出 chunk 移出池）** | 385 | **0.2330** | 0.2307 | 0.2211 | 0.0069 |

**四条结论**：

1. **锚定抬高 ≈ +0.66 MRR**（0.8956 vs 0.2330）：原基准报告的数字中
   **约 74% 来自"把已知答案排到前面"**。
2. **架构具备真实的跨域检索能力**：deanchor MRR **0.2330 = 随机基线的 34 倍**，
   Recall@10 = 0.3357（随机期望 ≈0.009）。若能力全来自锚定，
   deanchor 应落到随机水平——**故 §6.1"跨域检索"的核心主张成立，
   但支撑它的证据应从原 1.000（平凡饱和）换成此处的 34 倍提升。**
3. **权重重标定效应只在锚定口径下显现**（anchored +0.0565 / deanchor +0.0023）：
   原基准下"人工权重配错"的损失本身就发生在锚定路径上。
4. **训练增益在去锚定后反转**（anchored +0.005 → deanchor −0.012）：
   训练器学到的是"复现构造锚点"。

**对 §6.1 三通路分解的修正**：原表"语义超图通路召回 1.000"是候选池 ≤10 的
**平凡饱和**（池 ≤10 时 Recall@10 对任何返回全部候选的排序器恒为 1.0）。
其中**字面通路 0.000 与触发词级联通路 0.042 仍是真失效**（通路确实返回空），
但"1.000"不应作为性能成就报告。修复后的对应数字为 anchored Hits@10 = 0.9748、
deanchor Hits@10 = 0.4597。

**真实句向量下的稳健性（新增）**：在真实句向量（embedding-3，512 维；
`evaluate_repaired_real.py`）上复测，**锚定效应与向量器正交**：

| 向量器 | anchored MRR | deanchor MRR | 锚定效应 | deanchor / 随机 |
|---|---|---|---|---|
| 哈希（全量 634/385） | 0.8956 | 0.2330 | **+0.6626** | **33.8×** |
| 真实（子集 519/308） | 0.9066 | 0.2191 | **+0.6875** | **31.8×** |

两个向量器下锚定效应几乎相同（+0.66 vs +0.69），去锚定下均为随机的 30 倍以上
——**说明 §4.1/§6.7 的核心结论不依赖向量器选择**。但真实向量下**训练器的劣势
被放大**：deanchor 下 trained 0.0933 vs hand 0.2191（**−0.126**），
即训练器在真实表示上更严重地过拟合了锚定路径。

> 限制：真实向量复测仅在**查询文本已缓存**的子集上进行（超边描述命中率 100%，
> 查询文本命中率 81.9%）。全量复测需 EMBED_API_KEY 重取 115 条查询向量。

---

## 7 消融与负面结果

| 消融 | 预期 | 实测 | 结论 |
|---|---|---|---|
| A1 去类型安全约束 | P1 回升 >30% | P1 不变（拦 24 个错误绑定） | **H1 证伪**：约束管结构正确性（挂哪个框架），不管精度（要不要这条边）；精度由 refine+阈值承担 |
| A2 改回 LLM 聚类 | 成本升 10× | 0→187 次/千边 | H2 成立 |
| A3 三通道 | — | 无 refine P1 43.4% → 有 refine 0.0% | refine 是精度防线 |
| A4 去自举本体 | — | 召回降 −1.8pp | 价值在 P0 资源 |
| A5 去跨 chunk 扩展边 | 消歧下降 | P3 F1 1.000→0.000 | L1.5 是消歧的必要条件 |
| A6 换通用常识知识 | 性能下降 | 常识通路 0.095 vs 隐喻通路 0.783（n=652） | ✅ 专属隐喻知识不可替代 |
| A7 训练 vs 人工 | 训练更优 | 条件化（§6.2） | **H5 条件化成立** |
| A8 关自适应阈值 | — | 稀疏/稠密图均无差异 | 中性 |
| A9 去角色特征 | MRR 降 | 完全不变 | **H7 证伪** |

> **A7/A9 的口径警告（实测）**：`evaluate_fullcorpus` 上的 A7/A9 三臂结果
> **完全相同**（均 0.998），但这是**候选池饱和**所致——该基准每查询候选池
> min=5 / median=8 / max=10（**100% ≤10**），指标无区分力（见 §4.1）。
> 因此该表上的 A7/A9 结论**不可用于判定训练/特征的价值**；
> 有效数字应取 §4.1/§6.7 的修复后基准（全局池 1,100）。

**并行实验补充的负面结果（新增，均由独立分支实测并复核）**：

| 检验 | 预期 | 实测 | 结论 |
|---|---|---|---|
| 查询侧可观测标量 Ω | 预测级联通路失效 | ρ(Ω, 触发词命中数)=**0.9949**；控制集合大小后全部标量 AUC 覆盖 0.5；穷举 64 种合成组合 AUC 均落在 0.50–0.93 | **方向不可行**：只读查询的标量退化为 1-bit 触发词指示器；"触发词命中数"即天花板 |
| 降级来源封顶可靠度通道 | 让类型约束影响 P1 | P1 效应恒为 **0**；7/7 条字面误判句**全部**由本体正式框架支撑（0/7 来自降级边） | **结构性不可能**：P1 是句级布尔存在性指标，软通道只作用于排序 |
| HGNN 加源项 | 打破 flat/HGNN 平局 | 18 个 (α,ε) 配置下 \|ΔAUC\| ≤ 0.005，95% CI 跨 0 | **平局非算子退化所致**；源项只在 layers ≥ 50 有意义 |
| 更换级联构造规则 | 恢复 L3 组织能力 | 全单例对照与生产配置 MRR **完全相同**（0.5025）；仅彻底移除才降到 0.4681 | **L3 零结构贡献**：唯一因果通道是 `cascade_id` 非空这一布尔位 |
| 7 维特征的处置（删除/替换） | 提升排序 | 删除 `type`、删除 `same_cascade`、6 个替代信号——**全部 CI 覆盖 0** | 无显著效应；`type` 非死维但弱（配对 AUC 0.525） |

**其他诚实结论**：① 自举触发词 train→test 泛化差（召回涨 4.5 倍、误判同步涨
4 倍），自举价值在框架/级联结构；② 离线规则无廉价特征区分新喻体的隐喻/字面
身份判断；③ 语义域失谐通道在 LLM 在场时边际贡献仅 +0.6pp；④ 文本级延续性
判据（触发词复现/喻底交集≥2）无法过滤词化惯用语，扩展链质量必须语义校验
（§6.6）；⑤ LLM-as-judge 结论对 judge 模型敏感（§6.5）；⑥ **原检索基准的
"非构造金标"标签不成立**（100% 的金标含产出 chunk），且候选池过小使
Hits@10 平凡饱和——两处缺陷分别对应 IR 文献的 **pooling bias** 与
**test collection 规模不足**（§4.1）。

---

## 8 限制

1. **向量器双口径**：主表哈希向量可离线复现；真实句向量结果（+21pp）基于单一
   厂商单模型（embedding-3），转正前需统一重测并做多模型对照。
2. **金标来源与跨模型可复现性**：改写查询与相关性判定由同一 LLM 生成/判定
   （构造一致性 96.8%）；跨模型二评（glm-4.7 独立重判 13 查询/114 边对）
   Cohen's κ=**0.965**（近乎完美），金标跨模型可复现性得到量化支持；
   扩样与真人复核仍建议（第二标注者免费配额限制扩样进度）。
3. **扩展链精度**：双段管线后 63.6%（7/11 全量；跨模型配对子集 71.4%），
   校验与抽取同厂商；全 11 条已判定，人工抽查仍建议。
4. **A6 替代口径**：常识关联由 LLM 生成而非 ConceptNet 本体（502 阻塞）；该设计
   隔离了知识类型变量，但覆盖/噪声特性与真实常识图谱不同，恢复后应复测。
5. **语料边界**：CCL2018 为独立短句；连贯文档语料中鲁迅部分（1920 年代白话）
   L1 基数低（现代触发词表覆盖不足），跨语时代泛化是已知边界；跨语言（VUA）
   验证待英文通道建设。
6. **单语言**：框架与本体为中文；英文资源为对齐标注而非独立英文抽取本体。

---

## 9 结论

本文证明：隐喻的 n 元性可以被翻译为超超图结构并转化为可度量的检索收益——
在触发词捷径被切断的改写查询上，语义超图检索是唯一稳健的隐喻通路（1.000 vs
0.042/0.000），且常识关联替换专属级联后召回跌落 8 倍；LLM 开放发现 + 双防线
把抽取推至 69.6% 召回 / 9.2% 误判；级联查表让层级构建近乎免费；"候选生成 →
语义校验"双段管线使跨 chunk 扩展隐喻在真实文档上同时可用（密度 10×）且可靠
（精度 4×）。同样重要的是我们测得的边界：层级传播的收益以语义表示质量为前提，
训练排序器的增益以查询分布为条件，类型约束承担的是结构正确性而非精度，
LLM-as-judge 的结论以 judge 模型为转移。隐喻结构化检索的下一个问题，是在
连贯文档与真实句向量表示下把这四组条件化的结论整合为统一的检索理论。

---

## 附录 A：复现命令

| 表/实验 | 命令 |
|---|---|
| §5.1 | `python -m metaphor_graph.evaluate_real --use-metanet --use-bootstrap --use-semfield --use-llm --llm-batch 20 --llm-conf 0.85 --llm-cache data/llm_cache_deepseek.json` |
| §5.2 | `python -m metaphor_graph.ontology_clean --verify`；双语：`python -m metaphor_graph.ontology_bilingual --llm-complete` |
| §6.1/6.2 | `python -m metaphor_graph.evaluate_fullcorpus [--embedder real]` 与 `python -m metaphor_graph.evaluate_llmgold --stage eval [--embedder real]` |
| **§4.1/§6.7（修复基准）** | `python -m metaphor_graph.evaluate_repaired`（全局池 + anchored/deanchor 三臂对照） |
| **§6.7（真实向量复测）** | `python -m metaphor_graph.evaluate_repaired_real`（只读 embed 缓存，$0） |
| §6.3 | `python -m metaphor_graph.evaluate_hgnn [--embedder real]` |
| §6.4 A6 | `python -m metaphor_graph.evaluate_a6` |
| §6.5 | `python -m metaphor_graph.demo_rag`；`python -m metaphor_graph.evaluate_packing_ablation`；`python -m metaphor_graph.evaluate_llmasjudge` |
| §6.6 | `python -m metaphor_graph.evaluate_document_corpus --stage all [--continuity ... --llm-verify]`；`python -m metaphor_graph.evaluate_chain_quality [--llm-verify]` |
| §7 | `python -m metaphor_graph.ablation`、`python -m metaphor_graph.evaluate_retrieval` |
| 全部单测 | `python -m unittest metaphor_graph.test_metaphor_graph`（124 项） |
| 图表 | `python -m metaphor_graph.make_figures` → figures/fig1–fig5（§5.1/§6.1/§6.4/§6.2/§6.6） |

> 需要真实 LLM 的阶段（discover/refine/改写/判定/校验/关联/生成）均已落盘缓存于
> `data/`，重放零请求；缓存键为确定性 id，跨进程可复现（有单测护栏）。

## 附录 B：与方案验收标准的对照

| 里程碑 | 验收标准 | 实测 | 状态 |
|---|---|---|---|
| P0 | 种子 ≥300 框架 | 2,177（双语对齐 100%） | ✅ |
| P1 | P1 <15%（Go/No-Go） | 9.2% | ✅ |
| P2 | L2/L3 覆盖率 >85% | 报出口径 100% / **诚实口径 82.4%** | ⚠️ 条件性达标 |
| P3 | 跨 chunk 消歧 >70% | F1 1.000（诊疗集）+ 连贯文档密度/精度闭环 | ✅ |
| P4 | 抽取 F1 +5pp | 口径不匹配，改测真实贡献：校验信号 0.95 + 表示质量前提发现 | 🟡 已收口 |
| P5 | 6 组消融 | **全部完成**（A6 以 AI 替代口径补齐） | ✅ |
| P6 | 论文 | 本稿 v0.2 | ⏳ 译英/ polish |

## 附录 C：相关工作补充检索（A5，2026-08-31 联网核实）

投稿前复核研究缺口：检索确认**尚无"超图 RAG × 隐喻检测"交叉工作**（两线程
独立活跃）。新增引用候选（均已联网核实存在）：

1. Feng, Y. et al. (2026). Hyper-RAG: Combating LLM hallucinations using
   hypergraph-driven RAG. *Nature Communications*.（+12.3% over 直接 LLM；
   优于 GraphRAG/LightRAG 6.3%/6.0%）
2. HyperGraphRAG: Retrieval-Augmented Generation via Hypergraph. *NeurIPS 2025*.
3. Knowledge Is Not Static: Order-Aware Hypergraph RAG. arXiv 2026.
4. HyperRAG: Reasoning N-ary Facts over Hypergraphs. ACM 2025.
5. When to Use Graphs in RAG: A Comprehensive Analysis. *ICLR 2026*.
6. Fuoli, M. et al. (2025). Metaphor identification using large language models.
   arXiv:2509.24866.
7. HyperG / HyperLLM（arXiv 2502.18125 / 2510.11728）：超图 × LLM 框架。

**金标交叉审核预检（附录 A 外的独立证据）**：跨模型二评（glm-4.7 重判 13 查询）
与原金标 Cohen's κ=0.965（n=114 边对，1 处不一致为二评更宽）——LLM 非构造
金标的跨模型可复现性近乎完美；全量 50 查询扩样受免费配额限制，缓存支持增量。

