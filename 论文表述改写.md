# 论文表述改写（P0 落地版）

> 目的：把 `metaphor_graph/README.md` §7 中「试过且证伪」与「口径错配」的实验结论，
> 逐条翻译成论文里的正确主张。每条给出 **原表述 → 改写后表述 → 支撑证据**，
> 避免审稿人误读为「没试过」，也避免我们过度主张。
> 所有数字均来自仓库内可离线复现的脚本（复现命令见各证据行）。

---

## 一、创新点逐条改写（对应方案 §9.1）

### 创新点 1：隐喻超超图表示

- **原表述**：首个隐喻超超图表示，将 n 元性与层级性统一建模。
- **改写后**：首个隐喻超超图表示。**且我们的消融进一步指明了信号来源**：
  超边的 **n 元性**（喻底集合共现）承担了可检验的结构信号，而 **层级性**
  （框架/级联超顶点）的价值在于检索组织与可解释性（U-Retrieval 粗筛、
  框架一致性约束），不在检测/排序信号本身。
- **证据**：
  - H4 信号来源分解（`python -m metaphor_graph.evaluate_hgnn`）：同组配对样本上
    仅 L1 超边传播判别 0.950 = 完整跨层传播 0.950 > 原始嵌入 0.150；
    GRU 共成员序列 1.000。层级对检测信号零贡献。
  - P4-A（同上）：哈希向量下跨层传播 −0.104；**真实句向量（embedding-3）下翻案为
    +0.062（HGNN 1.000 vs 原始 0.938）**——层级传播的收益以语义表示质量为前提，
    这本身是一个可报告的发现。
  - 论文写法：把「n 元性 vs 层级性」作为**一个可分离的科学问题**来报告——
    这比笼统主张「超超图都好」更锋利、更可辩护。

### 创新点 2：类型安全约束

- **原表述**：用结构化规则过滤字面误判，不依赖 LLM 判断（P1 <15% 的主要保障）。
- **改写后**：类型安全约束保证 **L2/L3 结构正确性与跨框架一致性**（拦下 24 个
  类型不相干的错误绑定）；**字面误判过滤由 LLM refine 校验（43.4% → 0.0%）与
  置信阈值承担**。开放发现与封闭类型系统存在本质张力：每接受一个新映射就需
  登记一次类型（登记全部 → 约束恒真），要约束影响精度就必须丢弃候选 ——
  那正好抹掉开放发现的全部召回（+55.8pp）。**H1 证伪是结论，不是缺陷。**
- **证据**：A1（`python -m metaphor_graph.ablation`）：枚举约束拦 0 + 连贯性拦 24，
  P1 纹丝不动（0.092）；A3 通道消融：无 refine 时 P1 43.4%，有 refine 0.0%。

### 创新点 3：跨 chunk 扩展隐喻超边（L1.5）

- **原表述**：填补向量检索在延伸隐喻上的结构性盲区。
- **改写后**：同上，但**限定价值域**：L1.5 的优势出现在**连贯文档**（延伸比喻
  的延伸部分跨 chunk 分布的真实 RAG 场景）。在独立短句语料（CCL2018 每句独立）
  上扩展链天然稀疏（全量 1100 句仅成链 5 条/110 伪文档），不主张句级语料上的
  泛化召回增益。
- **证据**：A5（`python -m metaphor_graph.evaluate_retrieval`）：关扩展边后
  P3 F1 1.000 → 0.000（机制成立的必要条件证明）；全量建图
  （`python -m metaphor_graph.evaluate_fullcorpus`）：扩展链稀疏的测量。

### 创新点 4：cascade 本体查表降本

- **原表述**：L2/L3 构建成本降低一个数量级（~200 → ~20 次/千边）。
- **改写后**：**保留，定性成立**（H2）。补充本轮新证据：清洗后的生产本体
  （2177 框架，core/longtail 分层）经缓存重放验证与原始本体逐指标持平
  （P1 9.2%/召回 69.6%/覆盖率 100%），且 core-only 消融显示 longtail 单例框架
  贡献 +1.2pp 召回（模糊匹配锚点价值）。
- **证据**：A2（ablation.py）；§6.10 三变体验证（`python -m metaphor_graph.ontology_clean --verify`）。

### 创新点 5：超图版 KEG（HGNN）

- **原表述**：首次用超图消息传递建模非邻接成分的隐喻上下文（抽取 F1 +5pp）。
- **改写后**：**口径必须重定**（P4 原验收无法成立：HGNN 是检索/语义信号编码器，
  CCL2018 是逐句金标且无逐边金标，无法驱动图卷积或训练逐边检测器）。改述为：
  「HGNN 提供**图结构隐喻连贯度信号**（真隐喻边 vs 跨框架负样本配对判别 0.950），
  可作为触发词与 LLM 之外的**第三道校验**；该信号来自 L1 喻底的 n 元共现
  （H4 分解），对称跨层传播对检索排序无增益（P4-A −0.104）。」
- **证据**：`python -m metaphor_graph.evaluate_hgnn`（P4-A/P4-B/H4 三节）。

---

## 二、必须写进论文的负面结论清单

| # | 结论 | 数字 | 位置 |
|---|---|---|---|
| N1 | H1 类型约束不承担精度过滤（拦 0，修过仍不动） | P1 0.092 不变 | A1 |
| N2 | refine 校验才是 P1 的真正防线 | 43.4% → 0.0% | A3 |
| N3 | 语义域通道在 LLM 在场时边际贡献仅 +0.6pp | 12.4% − 11.8% | A3 |
| N4 | 自监督排序器在**触发词重叠型查询**下被 clue 单特征 AUC≈1.0 锁死（贴天花板 / 训练更差）；**但 H5 条件化翻案**：改写型查询（LLM 非构造金标，n=604）下训练排序器反超人工加权 +4.8pp MRR （0.545 vs 0.497）——增益取决于查询分布 | — | A7 + 全量复验 + §7.5 llmgold |
| N5 | 角色特征对排序无贡献 | 去除后 MRR 完全不变 | A9 + 全量复验 |
| N6 | 触发词级联式跨域检索对改写查询脆弱（稳健通路是语义超图检索） | 字面通路 0.000 vs 隐喻通路 0.500；语义超图 MRR 0.906 | H3 |
| N7 | 自适应阈值在稀疏与稠密（Δ=1.071）图上均测不出差异（中性，n 小） | 0.500 vs 0.500；0.250 vs 0.250 | A8 |
| N8 | 纯规则召回天花板 ~9.7%（P1<15% 约束下）；突破只能靠 LLM（+58.7pp） | §6.4–6.7 | evaluate_real |
| N9 | 自举触发词 train→test 泛化差（召回涨 4.5 倍、误判同步涨 4 倍）；自举价值在框架/级联结构不在触发词 | §6.9 表 | llm_ontology |
| N10 | 离线近似后端无廉价可判别特征区分新喻体的隐喻/字面身份判断 | 召回 23.1% 时 P1 14.5% | §6.6 |
| N11 | LLM 开放发现的 P1 依赖双防线（阈值 0.85 + refine），缺一道即超标 | 14.5% / 7.9% | §6.7 |

---

## 三、可直接使用的摘要段落（草稿）

> 我们提出 MetaphorSHG，将隐喻建模为承载源域、目标域、喻底集合与触发词的
> n 元超边，并以隐喻级联（cascade）组织为超顶点层级，用于检索增强生成。
> 与把隐喻压缩为二元映射对的知识图谱不同，超边的喻底集合天然表达隐喻的
> 多属性喻底；跨 chunk 扩展超边则修复延伸比喻被分块切断的问题。
> 在中文隐喻标注语料上的实验表明：(1) 接入 LLM 开放发现后，隐喻句召回从
> 规则侧的 9.7% 提升至 69.6%，而字面误判率保持 9.2%（硬门槛 15%）——
> 其中 LLM 置信阈值与 refine 校验两道防线缺一不可；(2) 由 cascade 本体
> 查表驱动的层级构建将 L2/L3 覆盖率提升至 100%，构建成本比 LLM 聚类
> 低一个数量级；(3) 图结构隐喻连贯度信号（跨框架配对判别 0.95）可作为
> 触发词与 LLM 之外的第三道校验，且消融显示该信号来自超边的 n 元喻底
> 共现而非层级传播本身；(4) 去掉跨 chunk 扩展超边后，延伸隐喻消歧 F1
> 从 1.000 降至 0；(5) 在触发词规避的改写查询上（LLM 独立判定的非构造金标，n=604），
> 触发词级联通路与字面通路召回分别仅 0.043 与 0.000，语义超图检索召回 1.000，
> 且训练排序器较人工加权提升 4.8pp MRR——检索增益集中于改写型查询分布。我们同时如实报告了多项被证伪或需条件化的假设（类型约束不过滤
> 字面误判、自监督排序器仅在改写型查询下有增益、层级传播对检索与检测信号无增益），
> 以期为后续隐喻结构化检索研究提供完整的证据边界。

---

## 四、Limitations 章节素材

1. 向量器双口径：§1–§5 主表基于哈希向量（可全离线复现）；真实句向量（embedding-3）
   已实测为 +21pp MRR / P4-A 翻案（`--embedder real`），转正为默认前须统一重测主表。
2. 评测语料局限：CCL2018 为独立短句（扩展链稀疏）；检索金标为 by-construction
   （自监督口径），只对排序/阈值组件有效；2 文档诊疗集证明机制而非泛化。
3. A6 未完成：ConceptNet 公开 API 实测 502（2026-08-30 双通道验证），
   通用常识图谱对照暂缺；方案引用的既有消融（MetaphorBoost 换 ConceptNet
   性能下降）为唯一间接证据。
4. 自举本体（2177 框架）来自单一模型单语料；双语对齐与跨语料泛化待做。
5. HGNN 未训练（无逐边金标可训）；P4-B 0.95 为 n=20 配对的判别准确率，
   样本量有限。
